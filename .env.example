# Copy this file to `.env` (same folder) and paste your real keys.
# .env is git-ignored and never read by Claude into chat.

# DeepSeek — platform.deepseek.com  (bulk generation node)
DEEPSEEK_API_KEY=

# Google Gemini — aistudio.google.com/apikey  (cross-family verifier)
GEMINI_API_KEY=

# Moonshot / Kimi — optional, inactive until set
# MOONSHOT_API_KEY=
