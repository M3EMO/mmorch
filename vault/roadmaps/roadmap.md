# Roadmap (curado — solo cambia con OK de Mateo)

El ARCHIVO es la fuente de verdad. Cualquier vía que lo mueva vale (edición a
mano o "dale la N" en sesión); el nightly registra outcomes comparando diffs y
detecta promociones de candidatas (`mmorch.fuel.detect_promotions`).

Consolidado 2026-08-18 desde la primera curación masiva (22 candidatas
promovidas — dedup de redundantes, agrupado por tema; trazabilidad en los
comentarios de cada línea).

## Direcciones

- **Entrenamiento de IA propia** — flywheel de datos (ya capturando: DPO,
  decisiones, traces) → router aprendido en shadow mode contra eval congelado
  (jamás toma control hasta ganarle al bandit) → reward model destilado del
  refutador → volumen mayor (sintéticos gateados + minería git con reward
  ejecutable + LoRA en GPU rentada; wayfinder antes de escalar).
  <!-- cand-2026-08-14-04, 14-05, 15-07b, 18-01, 18-02 -->

- **Calidad y anti-pseudo-progreso del loop** — SPC (CUSUM+EWMA) sobre
  outcomes; active learning en adjudicación (solo la franja incierta a juicio,
  disenso = prioridad); MAP-Elites por celda proyecto×tipo contra el
  mode-collapse; detector de divergencias engine/logs; refutaciones → tests de
  regresión semántica automáticos.
  <!-- cand-2026-08-15-05b, 15-06b, 15-08, 15-03, 15-04 -->

- **Grafts prime-agent** — rollback estructural (snapshot por edit + inversión
  mecánica) en evolve/close-loop; playbooks ejecutables (reference validado en
  session_skills); review-gate barato pre-persistencia del ingest.
  <!-- cand-2026-08-14-01, 14-02, 14-03 -->

- **Deuda estructural** — unificar los 3 ThompsonBandit (namespacing de brazos,
  evidencia compartida); feedback.jsonl → SQLite con índices cuando el volumen
  lo pida.
  <!-- cand-2026-08-15-01, 15-02 (misma idea, dedup) -->

- **Engine /project** — graft /speckit.analyze: verificación cross-family de
  consistencia spec↔decompose antes de construir.
  <!-- cand-2026-08-12-01 -->

- **Jarvis físico** — Home Assistant + sensores ESP32 vía MCP (medir el mundo,
  actuar con tool-use, percepción preentrenada); wayfinder HITL antes de
  comprar/construir.
  <!-- cand-2026-08-14-06 -->

<!-- Dedup de la curación 2026-08-18: cand-18-03 y 18-04 (variaciones
     router/refutador) absorbidas en "Entrenamiento"; 15-05 y 15-06 (variantes
     nightly de clasificadores sobre snapshots) absorbidas en "Calidad" y
     "Entrenamiento"; 15-07 (routing con feedback implícito) absorbida en
     "Entrenamiento". Nada se perdió: candidatos.md#Archivadas conserva todas
     con estado promovida. -->

<!-- Histórico pre-loop: INNOVATION_ROADMAP_2026-06-07.md (raíz del repo,
     archivado — todo LANDED, no migra nada). -->
- Añadir un mecanismo de 'canary semántico' en el ingest de sesiones: antes de persistir cualquier artefacto nuevo, ejecutar una verificación cruzada de bajo costo entre el contenido entrante y los snapshots históricos (de rollback estructural) para detectar regresiones semánticas sutiles que el refutador actual no captura porque opera sobre pares, no sobre series temporales.  <!-- cand-2026-08-19-01, dale 2026-08-21 -->
- Integrar el router aprendido (task signature -> modelo/umbral) como gate de entrada para el review-gate barato pre-persistencia: en lugar de siempre llamar shouldrefine, el router decide si el refinamiento necesita revisión antes de persistir, usando pseudo-labels del refutador cross-family para calibrar el umbral dinámicamente.  <!-- cand-2026-08-19-02, dale 2026-08-21 -->
- Añadir un 'filtro de ruido temporal' al ingest de sesiones: antes de persistir memoria, comparar el timestamp del evento con el contexto de la sesión activa; si el evento es redundante con uno reciente (misma firma semántica en <5 min), descartarlo sin llamar a shouldrefine (ahorro de costo y latencia). Esto complementa el gate barato existente, pero ataca la duplicación temporal, no la calidad del contenido.  <!-- cand-2026-08-19-03, dale 2026-08-21 -->
- graft de loushang: Sistema de sesiones persistentes con resume/fork/export — Un módulo de sesiones que serializa el estado completo de una conversación/ejecución (mensajes, contexto, herramientas, productos de trabajo) a disco, permitiendo reanudar, bifurcar y exportar. Incluye diagnóstico de integridad y recuperación ante corrupción. (aplica a mmorch: el orquestador multi-modelo necesita persistir el estado de las iteraciones de ideas, las decisiones del bandit y el historial de verificación cross-family para poder reanudar experimentos largos sin perder contexto.; ver nota minado-loushang-2026-08-19)  <!-- cand-2026-08-19-04, dale 2026-08-21 -->
- graft de loushang: Catálogo de modelos con registro y resolución por proveedor — Un registro central de modelos AI con metadatos (proveedor, capacidades, costo, límites de contexto) y una función de resolución que selecciona el modelo apropiado según la tarea, con fallback y listado de modelos disponibles. (aplica a mmorch: el orquestador necesita un catálogo unificado de modelos para el bandit multi-brazo, permitiendo registrar nuevos modelos sin modificar el núcleo y resolviendo el mejor modelo por tarea/costo.; ver nota minado-loushang-2026-08-19)  <!-- cand-2026-08-19-05, dale 2026-08-21 -->
- graft de loushang: Sistema de extensiones con hooks, tools y recursos dinámicos — Un mecanismo de extensión a nivel de proyecto donde código Python externo puede contribuir hooks (eventos del ciclo de vida), tools personalizadas, recursos dinámicos y comandos, sin modificar el núcleo. Incluye resolución de precedencia y colisiones. (aplica a mmorch: el flywheel de entrenamiento y los proyectos del dueño (portfolio, apps Tauri, sistemas de estudio) pueden extender el orquestador con tools específicas de dominio sin tocar el código base.; ver nota minado-loushang-2026-08-19)  <!-- cand-2026-08-19-06, dale 2026-08-21 -->
- Integrar un 'selector de lente adaptativo' en el ingest: antes de persistir, el sistema evalúa la firma semántica del artefacto entrante y selecciona dinámicamente el lente más barato que cubra el tipo de riesgo (canary semántico vs. filtro temporal vs. refutador cross-family), en lugar de aplicar todos secuencialmente, reduciendo latencia y costo en flujos de alta frecuencia.  <!-- cand-2026-08-19-07, dale 2026-08-21 -->
- mmorch/ablation.py: Acoplamiento oculto: _verify depende de _SKEPTIC_SYSTEM y _parse_verdict de patterns.py sin inyección [estructural] — Líneas 38-47: _verify importa _SKEPTIC_SYSTEM y _parse_verdict directamente de patterns.py. Si patterns.py cambia el formato del system prompt o el parser de veredictos, ablation.py se rompe silenciosamente sin error de importación. El principio de inyección de dependencias (seam + injection) exige que estos sean parámetros inyectables. Además, _SKEPTIC_SYSTEM es un nombre con prefijo _ (privado) que se está usando desde otro módulo — señal de que el acoplamiento no fue diseñado intencionalmente. (ver auditoria-mmorch_ablation.py-2026-08-19)  <!-- cand-2026-08-19-08, dale 2026-08-21 -->
- mmorch/ablation.py: by_case almacena artifact truncado como label, duplicando datos que el llamador ya tiene [estructural] — Línea 66: `by_case.append({"label": c.label or c.artifact[:30], ...})` — si el llamador ya tiene los casos en memoria (los pasó como argumento), almacenar el artifact truncado en el resultado duplica información. El llamador puede reconstruir el label desde `cases[i].label` o `cases[i].artifact[:30]`. Esto viola la regla de 'estado en memoria, no releer disco' — aunque aquí es 'no duplicar en memoria'. El resultado debería referenciar el índice del caso, no copiar el dato. (ver auditoria-mmorch_ablation.py-2026-08-19)  <!-- cand-2026-08-19-09, dale 2026-08-21 -->
- mmorch/ablation.py: División por cero en lat_avg cuando cases está vacío [bug] — Línea 76: `lat_avg=round(sum(lats) / n, 2) if n else 0.0` — el guard `if n` protege la división, pero `sum(lats)` sobre una lista vacía devuelve 0.0, lo cual es correcto. Sin embargo, la línea 74 `accuracy=round(correct / n, 4) if n else 0.0` tiene el mismo patrón y es correcta. El bug real está en la línea 76: si `cases` está vacío, `n=0`, y aunque el guard evita el ZeroDivisionError, el resultado `0.0` es engañoso — debería ser `None` o un valor que indique 'no data', no 0.0 que sugiere 0% de precisión. (ver auditoria-mmorch_ablation.py-2026-08-19)  <!-- cand-2026-08-19-10, dale 2026-08-21 -->
- Incorporar un 'detector de regresión de capacidad' post-persistencia: tras cada snapshot, comparar el rendimiento del sistema (latencia, tasa de acierto, cobertura de tareas) contra la línea base histórica usando el mismo mecanismo de canary semántico, pero sobre métricas operativas en lugar de embeddings; si se detecta degradación > umbral, activar automáticamente un rollback estructural parcial del último cambio.  <!-- cand-2026-08-20-01, dale 2026-08-21 -->
- mmorch/adjudicate.py: Relectura innecesaria de archivos del disco tras actualizar frontmatter [estructural] — En run_incremental, después de llamar a _update_frontmatter(note_path, strong_projects), se relee el archivo con Path(note_path).read_text() para recalcular el hash. Esto viola el principio ADR-0001 (estado en memoria, no releer disco). El llamador YA tiene note['content'] en memoria y _update_frontmatter podría devolver el nuevo contenido. Además, esta relectura es frágil: si el archivo cambia entre la escritura y la lectura (race condition), el hash calculado no corresponderá al contenido real que se escribió. (ver auditoria-mmorch_adjudicate.py-2026-08-20)  <!-- cand-2026-08-20-02, dale 2026-08-21 -->
- mmorch/adjudicate.py: Duplicación de lógica de reconstrucción de by_project [estructural] — by_project se reconstruye desde state['pairs'] al final de cada corrida, pero strong_results (que contiene los mismos datos en memoria) ya tiene toda la información necesaria. Se está duplicando la fuente de verdad: strong_results en memoria y state['by_project'] en disco. Si alguien modifica la lógica de filtrado de strong en un lugar pero no en el otro, los resultados divergirán silenciosamente. La reconstrucción desde pairs es correcta pero redundante — strong_results ya es la fuente en memoria. (ver auditoria-mmorch_adjudicate.py-2026-08-20)  <!-- cand-2026-08-20-03, dale 2026-08-21 -->
- mmorch/adjudicate.py: Acoplamiento oculto con el formato exacto de frontmatter [estructural] — _update_frontmatter asume que el frontmatter YAML se serializa con yaml.dump y que el cuerpo comienza inmediatamente después del segundo '---'. Si otro módulo escribe frontmatter con un formato diferente (por ejemplo, con '---' al final del frontmatter en la misma línea, o con BOM), esta función corromperá el archivo. El módulo no valida que el frontmatter parseado sea un diccionario antes de asignar frontmatter['applies_to'] — si yaml.safe_load devuelve una lista o string, fallará con TypeError en lugar de manejar el caso. (ver auditoria-mmorch_adjudicate.py-2026-08-20)  <!-- cand-2026-08-20-04, dale 2026-08-21 -->
- añadir un mecanismo de 'canary estructural' post-persistencia: tras cada snapshot de rollback estructural, ejecutar una verificación asíncónica de integridad de grafos (detectar ciclos no deseados, nodos huérfanos, o dependencias rotas entre artefactos) usando el log de workflow_obs.jsonl como fuente de verdad, y si se detecta una anomalía estructural, disparar automáticamente una reversión local al último snapshot válido — esto complementa el canary semántico pre-persistencia al atacar regresiones de topología, no de contenido, y aprovecha la auditoría existente sin costo adicional en el hot path.  <!-- cand-2026-08-21-01, dale 2026-08-21 -->
- Incorporar un 'contador de ciclos de vida' en los artefactos persistidos: antes de escribir cualquier producto de trabajo, registrar el número de veces que fue revisado/refutado y su historial de mutaciones; si un artefacto alcanza N revisiones sin cambio semántico neto (delta de embedding < umbral), marcarlo como 'estable' y priorizar su reutilización en el router, reduciendo llamadas innecesarias a shouldrefine y mejorando la eficiencia del bandit al explotar conocimiento ya validado.  <!-- cand-2026-08-21-02, dale 2026-08-21 -->
- Añadir un 'veredicto de continuidad' al pipeline de verificación: tras cada veredicto del refutador cross-family sobre un artefacto, registrar un hash del estado del sistema (configuración activa, versiones de módulos, umbrales dinámicos) junto con el resultado; al persistir un nuevo artefacto, comparar el hash de estado actual contra el histórico para detectar si el contexto de verificación cambió significativamente (p.ej., umbrales recalibrados, modelo reemplazado), lo que invalida veredictos previos y activa re-verificación selectiva solo de artefactos afectados, evitando falsas confianzas por deriva de configuración.  <!-- cand-2026-08-21-03, dale 2026-08-21 -->
- mmorch/arbitration.py: Relectura de disco en pending_recheck() cuando el llamador ya tiene los datos [estructural] — pending_recheck() llama a _read(path) que relee el archivo JSONL completo del disco. Pero el orquestador que llama a esta función típicamente ya tiene los registros en memoria (p.ej. después de llamar a stats() o a log()). Esto viola el ADR 0001 (estado en memoria, no releer disco). El módulo debería aceptar una lista de registros como parámetro opcional (p.ej. rows=) para que el llamador pueda pasar los datos que ya tiene, evitando I/O redundante. La firma actual fuerza una relectura innecesaria cada vez que se consulta la cola de re-check. (ver auditoria-mmorch_arbitration.py-2026-08-21)  <!-- cand-2026-08-21-04, dale 2026-08-21 -->
- mmorch/arbitration.py: Truncamiento silencioso de evidencia en log() [bug] — En log(), el parámetro evidence se trunca a 200 caracteres (evidence[:200]) sin advertencia. Si el orquestador pasa una evidencia más larga (p.ej. un probe con salida extensa), el registro guarda una versión incompleta. Luego, stats() calcula 'dismissed_without_evidence' basándose en si el campo está vacío, pero un registro con evidencia truncada a 200 chars sigue contando como 'con evidencia'. Esto puede inflar la tasa de descartes con evidencia y ocultar descartes que en realidad tenían evidencia insuficiente pero que fue cortada. El truncamiento debería al menos registrar un flag o el largo original. (ver auditoria-mmorch_arbitration.py-2026-08-21)  <!-- cand-2026-08-21-05, dale 2026-08-21 -->
- mmorch/arbitration.py: Acoplamiento oculto: el orquestador debe conocer el formato interno de los registros [principio] — El docstring dice 'El re-check es juicio del orquestador; acá solo se surfacea la cola'. Pero pending_recheck() devuelve dicts crudos con campos internos (ts, critique, verdict, reason, source, evidence, rechecked). El orquestador que hace el re-check debe saber que la clave 'evidence' existe y qué significa, y debe mutar el dict para marcar 'rechecked': True. Esto es un acoplamiento oculto: si el módulo cambia el formato interno (p.ej. renombra 'evidence' a 'proof'), el orquestador se rompe silenciosamente. El módulo debería exponer una función mark_rechecked(rec) o devolver objetos con métodos, no dicts crudos. (ver auditoria-mmorch_arbitration.py-2026-08-21)  <!-- cand-2026-08-21-06, dale 2026-08-21 -->
- Integrar el filtro de ruido temporal y el canary semántico en un pipeline unificado de pre-persistencia que además registre el 'costo de oportunidad' de cada bloqueo/descarte: en lugar de solo bloquear o descartar, calcular el ahorro de tokens/costo de cómputo evitado (por no llamar a shouldrefine) y el riesgo de pérdida de información (por descartar duplicados o bloquear regresiones), alimentando un dashboard de retroalimentación que ajuste los umbrales adaptativos en tiempo real según la relación costo-beneficio observada.  <!-- cand-2026-08-21-07, dale 2026-08-21 -->
- Añadir un 'pipeline de verificación de integridad semántica post-persistencia' que, tras cada snapshot de sesión (fork/export), ejecute una validación asincrónica de consistencia entre el manifiesto de dependencias y los artefactos referenciados, detectando referencias huérfanas o versiones desalineadas que puedan corromper la reanudación futura, complementando el canary pre-persistencia.  <!-- cand-2026-08-21-08, dale 2026-08-21 -->
- Integrar el filtro temporal y el canary semántico con el router aprendido para predecir la relevancia futura del artefacto: antes de persistir, el router no solo decide si se necesita revisión, sino que también estima la probabilidad de que el artefacto sea recuperado/utilizado en futuras sesiones (basado en patrones de acceso histórico por familia y firma semántica); si la probabilidad es baja y el artefacto no es candidato a revisión, se descarta directamente sin persistir, reduciendo ruido acumulativo y costo de almacenamiento.  <!-- cand-2026-08-21-09, dale 2026-08-21 -->
- mmorch/architecture.py: god_module_candidates relee archivos que import_graph ya parseó [estructural] — Líneas 47-48 vs 55-56: import_graph ya lee y parsea cada archivo con ast.parse(f.read_text()). Luego god_module_candidates vuelve a leer cada archivo con f.read_text() para contar LOC. Esto viola el ADR 0001-estado-en-memoria-no-releer-disco.md — el contenido ya está en memoria (o al menos el AST), pero se relee del disco. Para un repo con 50 módulos, son 50 lecturas redundantes de disco. Peor: si el archivo cambia entre la primera y segunda lectura (race condition en CI), el LOC contado no corresponde al AST parseado. Fix: hacer que import_graph devuelva también el LOC, o que god_module_candidates reciba los ASTs ya parseados. (ver auditoria-mmorch_architecture.py-2026-08-21)  <!-- cand-2026-08-21-10, dale 2026-08-21 -->
- mmorch/architecture.py: co_change_pairs cuenta commits vacíos como cambios [bug] — Líneas 88-96: Cuando un commit toca archivos fuera de mmorch/ (ej: docs/, tests/), el parser agrega un set vacío a commits. El filtro len(c) <= _SWEEP_MAX_FILES no elimina sets vacíos (0 <= 8 es true). Esto infla solo y junto con commits que no tocan ningún archivo relevante, distorsionando los ratios. Un commit que solo toca README.md produce actual = set() que se agrega a commits. Si un commit toca 1 archivo de mmorch/ y 10 de docs, el set tiene 1 elemento, pasa el filtro, y cuenta como co-cambio legítimo cuando en realidad fue un commit de docs con un fix incidental. Fix: filtrar sets vacíos antes del filtro de barrido: commits = [c for c in commits if c and len(c) <= _SWEEP_MAX_FILES] (ver auditoria-mmorch_architecture.py-2026-08-21)  <!-- cand-2026-08-21-11, dale 2026-08-21 -->
- mmorch/architecture.py: _mutates no detecta mutación vía setattr o globals()[name] [bug] — Líneas 130-141: El regex solo detecta NAME[...] = y NAME.metodo(). Pero Python permite mutar globals sin esas sintaxis: globals()['CONFIG'].update({...}) o setattr(sys.modules[__name__], 'CONFIG', {...}) no son detectados. Un test que haga globals()['SHARED_STATE'].clear() pasa desapercibido. Es un caso borde, pero el módulo se vende como 'chequeo mecánico determinista' — un falso negativo aquí es exactamente el tipo de contaminación que busca prevenir. Fix: añadir detección de globals()[ y setattr( con el nombre como string: globals_call = re.compile(rf"globals\(\)\s*\[\s*['\"]{name}['\"]\s*\]") (ver auditoria-mmorch_architecture.py-2026-08-21)  <!-- cand-2026-08-21-12, dale 2026-08-21 -->
