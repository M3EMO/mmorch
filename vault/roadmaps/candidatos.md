# Candidatas

## Vigentes

- **cand-2026-08-21-13** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: deuda | gist: Integrar el lente de deuda con el router aprendido: entrenar el clasificador chico no solo sobre feedback.jsonl sino sobre el delta entre lo que el sistema decidió hacer y lo que habría sido óptimo según el refutador cross-family, usando ese delta como señal de deuda técnica acumulada que el router debe minimizar explícitamente en su función de pérdida. >> 2026-08-21: Usar delta como peso de muestra en feedback.jsonl, priorizando errores donde router difiere de óptimo cross-family, sin cambiar arquitectura. >> 2026-08-22: Pesos de muestra: delta router vs óptimo cross-family como prioridad en feedback.jsonl, sin tocar arquitectura. >> 2026-08-23: Usar delta como peso puede explotar con ruido; aplicar clipping y normalización por percentil para estabilidad. >> 2026-08-24: Aplicar clipping por percentil al delta antes de usarlo como peso, evitando que outliers dominen el entrenamiento. >> 2026-08-25: Aplicar clipping por percentil también al delta negativo (router mejor que óptimo) para balancear pesos y evitar que errores fáciles dominen por bajo delta. >> 2026-08-29: Si delta router vs óptimo tiene signo negativo (router mejor), usar peso asimétrico decayente para no castigar mejoras, solo si delta > umbral de ruido. | estado: pendiente
- **cand-2026-08-21-14** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: capacidad | gist: Agregar capa de meta-aprendizaje sobre el router aprendido: usar los snapshots de rollback estructural como señal de 'contradicción detectada' para ajustar dinámicamente los umbrales del router, creando un loop cerrado entre capacidad de revertir y capacidad de clasificar. >> 2026-08-21: Entrenar el router con snapshots de rollback como pseudo-etiquetas negativas, ajustando umbrales dinámicamente para minimizar contradicciones estructurales. >> 2026-08-22: Entrenar con snapshots de rollback como negativos; ajustar umbrales por contradicción detectada, minimizando divergencia estructural. >> 2026-08-23: Crear meta-clasificador que predice probabilidad de rollback desde features del snapshot; usar esa probabilidad como umbral dinámico del router. >> 2026-08-24: Usar probabilidad de rollback como umbral dinámico: si P(rollback) > 0.7, bajar umbral de confianza del router para forzar revisión manual. >> 2026-08-25: Si P(rollback) > 0.7 y confianza del router < 0.3, enrutar a revisión manual inmediata sin esperar umbral dinámico, priorizando contradicciones estructurales críticas. >> 2026-08-29: Si P(rollback) > 0.7 y confianza < 0.3, enrutar a revisión manual; si P(rollback) > 0.9, revertir automáticamente sin revisión. | estado: pendiente
- **cand-2026-08-21-15** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: integracion | gist: Integrar el router aprendido con el review-gate barato: usar el clasificador chico entrenado sobre feedback.jsonl no solo para elegir modelo/umbral, sino también como filtro de ruido pre-persistencia, reemplazando el call shouldrefine por una predicción del router cuando la confianza sea alta, reservando LLM solo para casos ambiguos. >> 2026-08-21: Doble umbral: confianza alta predice y saltea review-gate; confianza media usa LLM; confianza baja persiste con flag de deuda. >> 2026-08-22: Doble umbral: confidence >0.9 predice y saltea review; 0.6-0.9 usa LLM; <0.6 persiste con flag deuda. >> 2026-08-23: Doble umbral: >0.9 predice y saltea review; 0.6-0.9 usa LLM; <0.6 persiste con flag deuda, usando predicción del router como filtro de ruido pre-persistencia. >> 2026-08-24: En confianza baja (<0.6), predecir también si el ruido es post-persistencia; si predicción indica ruido, enrutar a limpieza diferida en vez de persistir con flag deuda. >> 2026-08-25: Si confianza baja y predicción indica ruido, aplicar ventana temporal: no limpiar diferido si snapshot tiene menos de 5 minutos, evitando carreras. >> 2026-08-29: En confianza baja con ruido predicho, evaluar edad del snapshot antes de limpiar: si <5min, diferir 30s y reintentar una vez; si persiste, persistir con flag. | estado: pendiente
- **cand-2026-08-21-16** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/auto_repair.py: Violación del ADR 0001: relee nightly.jsonl del disco cuando el llamador YA tiene el record en memoria [estructural] — Líneas 63-66: `if rec is None:` lee nightly.jsonl del disco. El docstring (líneas 55-57) dice que nightly.py YA pasa `rec` en memoria. El fallback a disco es código muerto que viola el principio 'estado en memoria, no releer disco'. Si nightly.py cambia y deja de pasar `rec`, este módulo silenciosamente repara lo de ANOCHE en vez de lo de HOY — bug latente por acoplamiento oculto. Debería ser `rec: dict` obligatorio o fallar ruidosamente. (ver auditoria-mmorch_auto_repair.py-2026-08-21) >> 2026-08-21: Eliminar fallback a disco: hacer `rec` obligatorio o lanzar excepción clara si nightly.py no lo pasa, forzando actualización del contrato. >> 2026-08-22: Hacer `rec` obligatorio: si `rec is None`, lanzar excepción `TypeError` con mensaje claro, eliminando fallback silencioso y forzando actualización del contrato. >> 2026-08-23: Eliminar fallback: exigir `rec` obligatorio con `if rec is None: raise TypeError('nightly.py debe pasar rec')`, eliminando lectura de disco. >> 2026-08-24: Agregar test de regresión que simule nightly.py sin pasar rec y verifique TypeError, cubriendo el contrato roto silenciosamente. >> 2026-08-25: Si rec es None y nightly.jsonl existe, lanzar TypeError incluso si el archivo es válido, asegurando que el contrato se actualice explícitamente. >> 2026-08-29: Si rec es None pero nightly.jsonl existe y es válido, registrar warning con stack trace antes de lanzar TypeError para auditoría. | estado: pendiente
- **cand-2026-08-21-17** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/auto_repair.py: Path hardcodeado de Windows en gate_cmd rompe portabilidad [bug] — Línea 91: `--basetemp={Path(repo_dir).drive}/Users/map12/AppData/Local/Temp/claude/pt-repair` usa `.drive` (C:) y un path absoluto de un usuario específico. En Linux/macOS esto produce un path inválido como `/Users/map12/...` (con `.drive` vacío). El gate fallaría con error de pytest, bloqueando TODA reparación. Debería usar `tempfile.mkdtemp()` o `Path(tempfile.gettempdir()) / 'pt-repair'`. (ver auditoria-mmorch_auto_repair.py-2026-08-21) >> 2026-08-21: Reemplazar path hardcodeado con tempfile.gettempdir() y validar existencia del directorio antes de ejecutar gate_cmd. >> 2026-08-22: Reemplazar `--basetemp={Path(repo_dir).drive}/Users/map12/AppData/Local/Temp/claude/pt-repair` por `--basetemp={tempfile.gettempdir()}/pt-repair` y crear el directorio con `mkdir(parents=True, exist_ok=True)` antes de ejecutar gate_cmd. >> 2026-08-23: Reemplazar Path(repo_dir).drive por tempfile.gettempdir(); crear directorio con mkdir(parents=True, exist_ok=True) y validar accesibilidad antes de gate_cmd. >> 2026-08-24: Añadir fallback: si tempfile.gettempdir() no es accesible o no existe, usar mkdtemp() y loguear warning sin abortar gate_cmd. >> 2026-08-25: Si tempfile.gettempdir() no es accesible, intentar con mkdtemp(); si ambos fallan, abortar gate_cmd con error claro y loguear diagnóstico completo. | estado: pendiente
- **cand-2026-08-21-18** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/auto_repair.py: Estado de reparación se escribe ANTES del intento de automerge, perdiendo el resultado [bug] — Líneas 99-101: `atomic_write_json(state_path, state)` se ejecuta antes del bloque de automerge (líneas 104-112). Si `try_automerge` falla o mergea, ese resultado NO se persiste en `repair_state.json`. En la próxima corrida, `pick_finding` verá `result: 'built'` y `retry_after` futuro, pero no sabrá si el merge fue exitoso — el estado queda incompleto. El estado debería escribirse DESPUÉS del automerge, incluyendo `out['automerge']`. (ver auditoria-mmorch_auto_repair.py-2026-08-21) >> 2026-08-21: Mover atomic_write_json después del bloque try_automerge, incluyendo out['automerge'] en el estado persistido. >> 2026-08-22: Mover atomic_write_json después del bloque try_automerge, persistiendo out['automerge'] en repair_state.json. >> 2026-08-23: Persistir estado completo post-automerge en un solo write, incluyendo resultado merge y timestamp, evitando escritura intermedia parcial. >> 2026-08-24: En caso de fallo de automerge, persistir también el error y el estado del merge (intento, fallo, timestamp) en repair_state.json para trazabilidad. >> 2026-08-25: Si try_automerge lanza excepción, capturar stack trace y persistir en repair_state.json con estado 'merge_failed' y timestamp, antes de propagar. >> 2026-08-29: En caso de crash entre write inicial y automerge, persistir en el write final un flag 'interrupted: true' con timestamp si el proceso murió antes del bloque, para detectar estado incompleto. | estado: pendiente
- **cand-2026-08-21-19** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: integracion | gist: Integrar el refutador cross-family como señal de calidad en el entrenamiento del router aprendido, usando el delta entre la decisión real y la óptima como peso de muestra, pero extendiendo esto a un mecanismo de 'deuda de clasificación': cuando el router decide con alta confianza pero el refutador cross-family detecta contradicción estructural, registrar esa discrepancia como pseudo-etiqueta negativa para futuros entrenamientos, cerrando el loop entre capacidad de refutación y capacidad de clasificación sin cambiar la arquitectura. >> 2026-08-21: Agregar pseudo-etiquetas negativas solo cuando discrepancia cross-family persiste ≥2 ciclos, evitando ruido por contradicciones transitorias. >> 2026-08-22: Persistir discrepancia cross-family como pseudo-etiqueta negativa solo si persiste ≥2 ciclos consecutivos, filtrando contradicciones transitorias antes de registrar deuda de clasificación. >> 2026-08-23: Registrar deuda solo si discrepancia cross-family persiste ≥2 ciclos consecutivos, filtrando contradicciones transitorias antes de pseudo-etiquetar negativo. >> 2026-08-24: Aplicar decaimiento temporal a la deuda: si discrepancia persiste ≥2 ciclos, registrar; si se resuelve en ciclo 3, expirar con tasa 0.5. >> 2026-08-25: Si la deuda expira en ciclo 3, registrar expiración con tasa 0.5 solo si el refutador confirma consistencia en ese ciclo; si no, mantener deuda activa. >> 2026-08-29: Si la deuda expira en ciclo 3, registrar expiración con tasa 0.5 solo si el refutador confirma consistencia en ese ciclo; si no, mantener deuda activa. | estado: pendiente
- **cand-2026-08-21-20** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: notas-huerfanas | gist: Integrar el refutador cross-family como oráculo de verificación en el loop de auto-reparación: cuando auto_repair.py genere un parche, ejecutar adversarial_verify sobre el diff antes de persistir repair_state.json, usando el veredicto para decidir si el parche se marca como 'validado' o 'deuda' — y si el refutador encuentra contradicción con artefactos existentes, activar rollback estructural del parche y registrar el evento como pseudo-etiqueta negativa para el router aprendido, cerrando el loop entre reparación, refutación y aprendizaje. >> 2026-08-21: Usar veredicto adversarial_verify como pseudo-etiqueta negativa para router, activando rollback estructural solo si contradicción con artefactos existentes persiste ≥2 ciclos. >> 2026-08-22: Registrar veredicto adversarial_verify como pseudo-etiqueta negativa en feedback.jsonl; rollback estructural solo si contradicción persiste ≥2 ciclos. >> 2026-08-23: Persistir veredicto adversarial_verify como pseudo-etiqueta negativa en feedback.jsonl solo tras confirmar contradicción estructural persistente ≥2 ciclos, evitando etiquetas ruidosas. >> 2026-08-24: Registrar veredicto adversarial_verify como pseudo-etiqueta negativa solo si contradicción persiste ≥2 ciclos y además el diff toca archivos con dependencias cruzadas no resueltas por rollback. >> 2026-08-25: Si contradicción persiste ≥2 ciclos pero rollback falla por archivos bloqueados, marcar 'deuda_irreversible' y escalar a revisión manual. >> 2026-08-29: Si contradicción persiste ≥2 ciclos y rollback falla por archivos bloqueados, registrar deuda_irreversible con hash del diff y escalar a revisión manual en el mismo ciclo. | estado: pendiente
- **cand-2026-08-21-21** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/automerge.py: try_automerge relee el diff del disco violando ADR 0001 [estructural] — try_automerge llama a classify_branch que ejecuta git diff y git show (3+ subprocess calls). El caller ya tiene el estado del repo en memoria. El módulo debería recibir el diff como parámetro en lugar de releer del disco, violando el principio 'estado en memoria, no releer disco' del ADR 0001. (ver auditoria-mmorch_automerge.py-2026-08-21) >> 2026-08-22: Refactorizar try_automerge para recibir diff como parámetro, eliminando llamadas a git diff/show y validando contra ADR 0001. >> 2026-08-23: Eliminar llamadas a git diff/show en classify_branch; recibir diff ya calculado como parámetro, validando contra ADR 0001. >> 2026-08-24: Agregar test de regresión que verifique try_automerge acepte diff como parámetro y falle si recibe None, sin releer disco. >> 2026-08-25: Si classify_branch recibe diff como bytes vacíos, validar que no sea None pero permitir merge trivial, sin llamar git show. >> 2026-08-29: Si classify_branch recibe diff como parámetro y es None, lanzar TypeError con mensaje claro y registrar stack trace antes de fallar. | estado: pendiente
- **cand-2026-08-21-22** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/automerge.py: red_content_hits marca archivos nuevos con fixtures de tests como rojos [bug] — En classify_branch, para archivos con status 'A' (nuevos), se pasa baseline='' a red_content_hits. Si un test nuevo contiene strings como 'password' o 'secret' en fixtures (común en tests), se marcará como rojo incorrectamente. El semáforo debería ignorar contenido rojo en archivos nuevos que son tests, ya que no pueden afectar producción. (ver auditoria-mmorch_automerge.py-2026-08-21) >> 2026-08-22: En classify_branch, omitir red_content_hits para archivos con status 'A' y rutas de test/fixture, evitando falsos rojos. >> 2026-08-23: En classify_branch, para status 'A', detectar rutas test/fixture con regex y skipear red_content_hits >> 2026-08-24: Para status 'A', detectar tests/fixtures solo si la ruta contiene 'test' o 'fixture' precedida por separador, evitando falsos positivos en archivos como 'contest.py'. >> 2026-08-25: Si ruta empieza con 'test' o 'fixture' en primer segmento, también omitir red_content_hits, no solo segmentos internos. >> 2026-08-29: Si status 'A' y ruta es 'tests/fixtures' exacta, también omitir red_content_hits; no solo segmentos internos. | estado: pendiente
- **cand-2026-08-21-23** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/automerge.py: Acoplamiento oculto con evolve._RED_PATHS y red_content_hits [principio] — El módulo importa _RED_PATHS (privado) y red_content_hits de mmorch.evolve dentro de la función. Si evolve cambia la firma de red_content_hits o renombra _RED_PATHS, este módulo revienta silenciosamente. Debería haber una interfaz pública en evolve que encapsule la lógica del semáforo. (ver auditoria-mmorch_automerge.py-2026-08-21) >> 2026-08-22: Extraer API pública `evolve.semaphore()` que encapsule `_RED_PATHS` y `red_content_hits`, usándola en automerge.py y eliminando imports privados. >> 2026-08-23: Exponer `evolve.analyze_semaphore(red_content_hits)`; hacer `_RED_PATHS` configurable vía parámetro con default, eliminando import privado. >> 2026-08-24: Exponer `evolve.analyze_semaphore()` que retorne también el índice del primer archivo rojo, permitiendo a automerge.py abortar temprano si el semáforo detecta conflicto antes de procesar el batch. >> 2026-08-25: Si _RED_PATHS no se importa por estar vacío, que analyze_semaphore retorne semáforo abierto sin iterar, evitando crash por lista vacía en automerge. >> 2026-08-29: Que analyze_semaphore acepte opcionalmente un set de _RED_PATHS override para testing, sin importar privados; si vacío, retornar semáforo abierto. | estado: pendiente
- **cand-2026-08-22-01** | fecha: 2026-08-22 | vence: 2026-09-05 | lente: deuda | gist: Agregar un mecanismo de 'deuda de validación' al router aprendido: cuando el refutador cross-family detecta una contradicción estructural en un snapshot de rollback, registrar no solo el delta entre decisión real y óptima, sino también el costo de oportunidad de no haber ejecutado el rollback antes (tiempo transcurrido entre la decisión inicial y la corrección), usando ese intervalo como peso temporal en el entrenamiento del clasificador chico, priorizando así errores que se arrastraron más tiempo. >> 2026-08-22: Ponderar pérdida del clasificador con intervalo rollback-decisión, priorizando errores de mayor latencia, sin alterar arquitectura. >> 2026-08-23: Multiplicar pérdida por (1 + log(1 + latencia)) con latencia en minutos, clip a 48h, y normalizar por percentil 95. >> 2026-08-24: Si latencia > 48h, registrar en log y excluir muestra del entrenamiento, evitando pesos extremos post-clip. >> 2026-08-25: Si latencia exacta es 0 minutos (rollback inmediato), excluir muestra del entrenamiento para evitar peso log(1)=0 que no aporta gradiente. >> 2026-08-29: Si latencia negativa por desync clocks, usar valor absoluto y loggear warning para auditoría de infraestructura. | estado: pendiente
- **cand-2026-08-22-02** | fecha: 2026-08-22 | vence: 2026-09-05 | lente: capacidad | gist: agregar un lente de 'capacidad de reversión informada' al router aprendido: antes de que el router decida con alta confianza y saltee el review-gate, ejecutar una verificación barata de reversibilidad estructural (si la decisión genera un cambio en archivos de estado/estructura, estimar si el rollback estructural sería trivial o costoso) y usar esa estimación como un tercer umbral dinámico: si la reversión es trivial, permitir confianza alta sin llm; si es costosa, exigir llm incluso con confianza alta. Esto convierte la capacidad de revertir en una señal proactiva de riesgo, no solo reactiva post-rollback. >> 2026-08-22: Precalcular costo de reversión por tipo de cambio (schema vs data) y cachear en tabla hash; router consulta O(1) antes de saltar review-gate. >> 2026-08-23: Cachear costo de reversión por tipo de cambio en hash; consultar O(1) antes de saltar review-gate, usando costo como umbral dinámico adicional. >> 2026-08-24: Cachear costo de reversión en hash consultado O(1), y si costo es alto, registrar el hash del estado pre-cambio para rollback diferido sin LLM. >> 2026-08-25: Si costo de reversión alto y hash de estado pre-cambio ya cacheado, usar ese hash para verificar integridad antes de rollback diferido, evitando corrupción. >> 2026-08-29: Si costo de reversión alto y hash pre-cambio cacheado, verificar también que el snapshot de estado no esté corrupto comparando checksum doble antes de rollback diferido. | estado: pendiente
- **cand-2026-08-22-03** | fecha: 2026-08-22 | vence: 2026-09-05 | lente: notas-huerfanas | gist: Integrar el router aprendido con el refutador cross-family en modo 'arbitraje diferido': cuando el router predice con alta confianza pero el refutador cross-family detecta contradicción estructural, en lugar de registrar deuda, activar un re-entrenamiento incremental del clasificador chico en caliente, usando el caso como pseudo-etiqueta negativa y ajustando el umbral de confianza dinámicamente, creando un loop de auto-corrección que reduce errores correlacionados del ensemble. >> 2026-08-23: Implementar reentrenamiento en caliente con mini-batch limitado a 64 ejemplos, solo si la contradicción estructural supera umbral de severidad, evitando sobreajuste. >> 2026-08-24: En reentrenamiento en caliente, aplicar decaimiento de learning rate por lote si la contradicción estructural persiste tras 3 mini-batches, evitando oscilación. >> 2026-08-25: Si contradicción estructural persiste tras 3 mini-batches en caliente, pausar reentrenamiento y registrar caso en cola de revisión manual, evitando loop infinito. >> 2026-08-29: Si contradicción estructural persiste tras 3 mini-batches en caliente pero la severidad baja de umbral, continuar reentrenamiento con batch de 16 y tasa fija, evitando pausa innecesaria. | estado: pendiente
- **cand-2026-08-22-04** | fecha: 2026-08-22 | vence: 2026-09-05 | lente: self-audit | gist: mmorch/autoresearch.py: Violación de ADR-0001: fpath.read_text() se llama 3 veces cuando el contenido ya está en memoria [estructural] — Línea 83: 'base_content = fpath.read_text()' lee el archivo. Línea 99: 'cur = ctx.best if ctx.best is not None else base_content' — si ctx.best existe, ya tenemos el contenido en memoria pero propose() escribe 'cur' al disco (línea 100) y luego score() lo relee implícitamente vía el scorer. Peor: línea 118 'fpath.write_text(res.best)' escribe el resultado final, pero si el scorer ya modificó el archivo (efecto secundario), el write sobreescribe sin verificar. El contenido del archivo se lee/escribe 4+ veces cuando el llamador ya tiene 'base_content' en memoria. Debería pasarse el contenido como parámetro o cachearse en el closure. (ver auditoria-mmorch_autoresearch.py-2026-08-22) >> 2026-08-24: Cachear contenido leído en variable local y pasarlo a propose() y score() como parámetro, eliminando relecturas y sobreescrituras sin verificar. >> 2026-08-25: Si ctx.best existe, pasar cur directamente a score() sin escribir a disco, y verificar mtime antes del write final. >> 2026-08-29: Si fpath cambia entre lectura y write final, detectar mtime y si difiere, fusionar con base_content en vez de sobrescribir. | estado: pendiente
- **cand-2026-08-22-05** | fecha: 2026-08-22 | vence: 2026-09-05 | lente: self-audit | gist: mmorch/autoresearch.py: Acoplamiento oculto: el scorer puede modificar target_file y run_autoresearch no lo detecta [estructural] — Línea 118: 'fpath.write_text(res.best)' asume que el archivo no cambió durante la corrida. Pero el scorer_cmd se ejecuta con cwd=cwd y shell=True — si el scorer (o cualquier script que corra) toca target_file, el resultado final sobreescribe silenciosamente esos cambios. Peor: propose() escribe el candidato al archivo (línea 100) ANTES de que score() corra, así que el scorer VE el candidato — pero si el scorer lo modifica, la siguiente ronda de propose() lee 'ctx.best' (en memoria, no del disco) y escribe eso, perdiendo la modificación del scorer. Este es un acoplamiento oculto: run_autoresearch depende de que el scorer NO toque target_file, pero no hay ninguna verificación ni documentación de ese contrato. (ver auditoria-mmorch_autoresearch.py-2026-08-22) >> 2026-08-23: Agregar hash de target_file antes/después de scorer_cmd; si cambió, registrar deuda técnica y restaurar versión original desde memoria. >> 2026-08-24: Agregar test de regresión que simule scorer modificando target_file durante ejecución, verificando que el hash detecte el cambio y restaure desde memoria. >> 2026-08-25: Si el scorer modifica target_file y el hash lo detecta, restaurar desde memoria y además registrar el diff exacto en feedback.jsonl para debugging. >> 2026-08-29: Si el scorer modifica target_file pero también cambia su tamaño a 0 bytes, detectar hash y restaurar, registrando diff y flag de corrupción. | estado: pendiente
- **cand-2026-08-22-06** | fecha: 2026-08-22 | vence: 2026-09-05 | lente: self-audit | gist: mmorch/autoresearch.py: resume_from_journal lee el archivo DOS veces y calcula 'best' de forma inconsistente [bug] — Líneas 44-55: la función primero itera sobre todas las líneas acumulando 'best' con lógica 'best = bs if best is None else best' (que en realidad NO actualiza best con valores mayores, solo mantiene el primero), y luego relee el archivo completo para tomar el último registro. El primer loop es código muerto que nunca actualiza 'best' correctamente — si el primer registro tiene best_score=0.5 y el segundo 0.9, 'best' queda en 0.5 hasta que el segundo bloque lo sobreescribe con 0.9. El primer loop debería eliminarse o corregirse; actualmente es confuso y sugiere que se intentó hacer un max() pero se implementó mal. (ver auditoria-mmorch_autoresearch.py-2026-08-22) >> 2026-08-23: Eliminar el primer loop muerto; unificar lógica de 'best' con max() y single-read del archivo. >> 2026-08-24: Envolver el segundo read en try/except FileNotFoundError con retorno de lista vacía y log advertencia, evitando crash si archivo se borra entre lecturas. >> 2026-08-25: Si la segunda lectura falla por FileNotFoundError, reintentar una vez con backoff de 100ms antes de retornar lista vacía; si persiste, loguear advertencia con timestamp y path. >> 2026-08-29: Si el archivo se borra entre lecturas y el retry falla, retornar lista vacía solo si el archivo no existía al inicio; si existía, loguear error crítico. | estado: pendiente
- **cand-2026-08-23-01** | fecha: 2026-08-23 | vence: 2026-09-06 | lente: deuda | gist: Incorporar el lente de deuda en el ciclo de auto-reparación: usar el delta entre el resultado esperado del repair y el resultado real (verificado por el refutador cross-family) como señal para actualizar los pesos de las reglas de reparación, priorizando las que más reducen deuda acumulada y penalizando las que la aumentan. >> 2026-08-23: Ponderar actualización de pesos por deuda acumulada con decaimiento temporal y normalización por percentil, evitando sobreajuste a ruido reciente. >> 2026-08-24: Usar delta acumulado como señal de gradiente: si repair aumenta deuda en ciclo t, aplicar factor de penalización 2x a pesos de esa regla con decaimiento exponencial. >> 2026-08-25: Si delta de deuda es negativo pero |delta| < umbral de ruido, no actualizar pesos; usar desviación estándar móvil como umbral. >> 2026-08-29: Si delta deuda es positivo por 3 ciclos consecutivos, congelar pesos de esa regla y escalar a revisión manual. | estado: pendiente
- **cand-2026-08-23-02** | fecha: 2026-08-23 | vence: 2026-09-06 | lente: capacidad | gist: Integrar el lente de capacidad con el sistema de auto-reparación: usar el historial de fallos en logs/feedback.jsonl y los resultados de ablation-18.4 para que el router aprendido no solo clasifique qué modelo usar, sino que también prediga la probabilidad de éxito de una reparación antes de intentarla, evitando ciclos de retry costosos y priorizando reparaciones con mayor chance de pasar el gate. >> 2026-08-23: Entrenar el router con salidas de auto-reparación: si falla, etiquetar negativa; si éxito, positiva, ajustando predicción de éxito y evitando reintentos costosos. >> 2026-08-24: Clasificar fallos por tipo (estructural, lógico, deuda) en el historial; el router predice éxito por tipo de reparación, priorizando las de mayor tasa histórica. >> 2026-08-25: Si la predicción de éxito de reparación es alta (>0.8) pero el tipo de fallo es estructural, aplicar descuento del 20% antes de priorizar. >> 2026-08-29: Si fallo estructural con predicción >0.8 y historial de reparaciones similares <3 casos, aplicar descuento adicional del 50% por baja evidencia. | estado: pendiente
- **cand-2026-08-23-03** | fecha: 2026-08-23 | vence: 2026-09-06 | lente: integracion | gist: Integrar el router aprendido con el mecanismo de auto-repair: cuando el router predice baja confianza (deuda técnica), en lugar de persistir con flag, activar automáticamente el pipeline de reparación (gate_cmd) para los casos donde el refutador cross-family detectó divergencia estructural, usando el resultado de la reparación como retroalimentación inmediata para actualizar el clasificador chico. >> 2026-08-23: Agregar loop: si reparación auto-repair exitosa, re-etiquetar sample como positivo para router, actualizando clasificador en caliente. >> 2026-08-24: Si reparación exitosa, re-etiquetar como positivo Y actualizar clasificador en caliente con el diff reparado como feature, no solo la etiqueta binaria. >> 2026-08-25: Si auto-repair exitosa y diff reparado es idéntico al original, no re-etiquetar positivo; marcar como 'no-op' y mantener clasificador sin actualizar. >> 2026-08-29: Si auto-repair exitosa pero diff reparado es igual al original, marcar 'no-op' y NO actualizar clasificador; solo registrar métricas de confianza. | estado: pendiente
- **cand-2026-08-23-04** | fecha: 2026-08-23 | vence: 2026-09-06 | lente: notas-huerfanas | gist: Integrar el lente de notas-huérfanas con el router aprendido: usar el clasificador chico entrenado sobre feedback.jsonl para detectar archivos huérfanos emergentes (baja conectividad entrante) como señal de deuda estructural temprana, priorizando su revisión en el ciclo de auto-reparación antes de que se conviertan en acumulación silenciosa. >> 2026-08-23: Priorizar revisión de huérfanos detectados solo si baja conectividad persiste ≥2 ciclos, filtrando ruido transitorio. >> 2026-08-24: Agregar test de regresión que simule 2 ciclos consecutivos con baja conectividad y verifique que el huérfano se priorice solo en el segundo ciclo. >> 2026-08-25: Si baja conectividad persiste ≥2 ciclos pero el archivo es editado en medio, resetear contador y no priorizar. >> 2026-08-29: Si baja conectividad persiste ≥2 ciclos pero el archivo crece en tamaño >50%, resetear contador y no priorizar. | estado: pendiente
- **cand-2026-08-23-05** | fecha: 2026-08-23 | vence: 2026-09-06 | lente: self-audit | gist: mmorch/babel.py: Acoplamiento oculto: ingest() escribe directamente en VAULT global sin inyección [estructural] — ingest() usa VAULT importado de .vault como destino fijo. El self-check parchea globals()['VAULT'] para evitar tocar el vault real, pero esto es frágil: cualquier otro módulo que importe VAULT directamente (no vía globals) rompería el aislamiento. La regla ADR-0001 (estado en memoria, no releer disco) se viola al escribir directamente sin pasar por una capa de servicio que gestione el estado. (ver auditoria-mmorch_babel.py-2026-08-23) >> 2026-08-24: Inyectar un servicio de estado como dependencia en ingest(), evitando acceso directo a VAULT; usar factory con override para tests. >> 2026-08-25: Si ingest() es llamado desde un worker con VAULT ya inyectado, verificar que el servicio inyectado sea el mismo que el global, lanzando AssertionError si difieren. >> 2026-08-29: Si VAULT inyectado difiere del global, lanzar AssertionError solo si ambos no son None; si global es None, permitir override sin error. | estado: pendiente
- **cand-2026-08-23-06** | fecha: 2026-08-23 | vence: 2026-09-06 | lente: self-audit | gist: mmorch/babel.py: Duplicación de lógica de chunking entre encode() y _chunks() [estructural] — encode() decide si chunkear basándose en len(text) > CHUNK_CHARS, y luego _chunks() vuelve a verificar si hay más de un chunk. La lógica de decisión está duplicada: si _chunks() devuelve un solo chunk (porque el texto es indivisible), encode() lo procesa directo, pero la condición de entrada ya garantizaba que era grande. Esto crea dos fuentes de verdad sobre cuándo chunkear. (ver auditoria-mmorch_babel.py-2026-08-23) >> 2026-08-24: Extraer la condición de chunking a un único método `_should_chunk(text)` usado por encode() y _chunks(), eliminando la doble verificación. >> 2026-08-25: Extraer condición a _should_chunk(text) y en _chunks() verificar si text es indivisible por límites de sentencia, no solo por longitud. >> 2026-08-29: _should_chunk(text) debe retornar false si len(text) <= CHUNK_CHARS o si text no contiene límites de sentencia divisibles, unificando ambas condiciones en una sola fuente de verdad. | estado: pendiente
- **cand-2026-08-23-07** | fecha: 2026-08-23 | vence: 2026-09-06 | lente: self-audit | gist: mmorch/babel.py: Chunking pierde el último chunk si el texto termina exactamente en el límite [bug] — En _chunks(), si el último párrafo completa exactamente el límite (len(cur) + len(p) == limit), se agrega a 'out' pero 'cur' se resetea a p, y al final del loop 'cur' contiene el último párrafo que ya fue agregado, resultando en duplicación o pérdida. El código debería verificar si cur ya fue agregado antes del append final. (ver auditoria-mmorch_babel.py-2026-08-23) >> 2026-08-24: Renombrar la variable 'cur' a 'pending' y agregar flag 'flushed' para evitar doble append en _chunks(). >> 2026-08-25: Si cur ya fue agregado a out, no resetearlo a p; solo agregar p si pending no está vacío, evitando duplicación. >> 2026-08-29: En _chunks(), si len(cur) + len(p) == limit, agregar p a out y setear cur a '' (vacío), no a p; verificar cur no vacío antes del append final. | estado: pendiente
- **cand-2026-08-24-01** | fecha: 2026-08-24 | vence: 2026-09-07 | lente: capacidad | gist: Integrar el lente de capacidad con el mecanismo de rollback estructural: usar los snapshots de rollback como señal de 'sobrecapacidad' (el sistema intentó algo que no pudo sostener) para ajustar dinámicamente la complejidad máxima permitida del código que el router puede aprobar, creando un techo adaptativo que evite acumular deuda técnica por ambición excesiva. >> 2026-08-24: Usar frecuencia de rollbacks por módulo como señal para reducir complejidad máxima permitida solo en ese módulo, no globalmente. >> 2026-08-25: Si frecuencia de rollbacks por módulo supera umbral, reducir complejidad máxima permitida solo para ese módulo, con decaimiento temporal automático. >> 2026-08-29: Si rollbacks por módulo superan umbral pero el módulo es crítico (alta fan-in), reducir complejidad solo tras 2 ciclos consecutivos, no al primero. | estado: pendiente
- **cand-2026-08-24-02** | fecha: 2026-08-24 | vence: 2026-09-07 | lente: self-audit | gist: mmorch/bench.py: get_task relee TASKS del disco cuando el llamador ya tiene la lista en memoria [estructural] — get_task(name) hace una búsqueda lineal sobre TASKS cada vez que se llama. Si un llamador ya tiene la lista de tasks (p.ej. de selection_tasks() o de una iteración), get_task vuelve a recorrer la lista global. Esto viola el principio de 'estado en memoria — no releer disco' (docs/adr/0001): TASKS es una constante global en memoria, pero get_task la trata como si fuera una fuente externa. Peor: si otro módulo mutara TASKS (p.ej. agregando tasks dinámicamente), get_task devolvería resultados inconsistentes con lo que el llamador ya tiene. La solución sería que get_task acepte la lista como parámetro opcional, o que los llamadores usen un dict indexado por nombre construido una sola vez. (ver auditoria-mmorch_bench.py-2026-08-24) >> 2026-08-25: get_task debería aceptar un índice opcional (dict nombre->task) construido una sola vez por el llamador; si no se pasa, construirlo con caché y validar consistencia con TASKS. >> 2026-08-29: get_task debe aceptar dict opcional y validar que todas las claves del llamador existan en TASKS, lanzando KeyError descriptivo si falta alguna. | estado: pendiente
- **cand-2026-08-24-03** | fecha: 2026-08-24 | vence: 2026-09-07 | lente: self-audit | gist: mmorch/bench.py: Bug en test de acceptance: dedup por id no coincide con la spec del task [bug] — En test_normalize_titlecase_dedup_negativos, la spec dice 'dedup por id (queda el primero)' pero el test usa dos filas con id='1' y montos 10.005 y 5.0, esperando len(rows)==1. Sin embargo, la primera fila tiene monto 10.005 que al redondear a 2 decimales da 10.0 o 10.01 (el test acepta ambos), pero la segunda fila con id='1' y monto 5.0 debería ser descartada por dedup. El problema: si el workflow implementa dedup ANTES de redondear, la primera fila (10.005) se queda y la segunda (5.0) se descarta — correcto. Pero si redondea primero, 10.005 → 10.0 y 5.0 → 5.0, y el dedup por id='1' descarta la segunda — también correcto. El bug real: el test no verifica que 'queda el primero' — solo verifica que queda UNO. Un workflow que dedupe quedándose con el ÚLTIMO (no el primero) pasaría este test, violando la spec. El test debería verificar que rows[0]['monto'] == 10.0 o 10.01 (el primero), no solo len(rows)==1. (ver auditoria-mmorch_bench.py-2026-08-24) >> 2026-08-25: Agregar aserción de que rows[0]['monto'] sea 10.0 o 10.01 para verificar que se conserva el primero, no solo len(rows)==1. | estado: pendiente
- **cand-2026-08-24-04** | fecha: 2026-08-24 | vence: 2026-09-07 | lente: self-audit | gist: mmorch/bench.py: Acoplamiento oculto: materialize depende del intérprete de Python del proceso mmorch [principio] — En materialize, el comentario dice 'El cmd corre con EL intérprete de mmorch' y usa sys.executable. Esto crea un acoplamiento oculto: si el workflow que consume este accept_cmd se ejecuta en un entorno donde sys.executable no está disponible (p.ej. un contenedor sin el mismo Python, o un worker remoto), el comando fallará silenciosamente. El principio de inyección de dependencias (seam + injection) dice que las dependencias deben inyectarse, no capturarse del entorno. La solución sería que materialize acepte un parámetro python_path opcional, o que el llamador pase el intérprete explícitamente. Además, el reemplazo 'python ' → f'"{sys.executable}" ' es frágil: si accept_cmd no empieza con 'python ' (p.ej. 'python3 -m pytest'), el replace no hace nada y el comando usa el python del sistema. (ver auditoria-mmorch_bench.py-2026-08-24) >> 2026-08-25: Si sys.executable es None o vacío, fallback a 'python3' con warning logueado, y aplicar replace solo a prefijo exacto 'python ' o 'python3 '. >> 2026-08-29: Si sys.executable es vacío y fallback a 'python3', validar existencia del binario con shutil.which antes de usarlo; si no existe, registrar error y abortar con TypeError. | estado: pendiente
- **cand-2026-08-25-01** | fecha: 2026-08-25 | vence: 2026-09-08 | lente: deuda | gist: Incorporar el delta entre predicción del router y desenlace real del feedback como feature de entrada adicional al clasificador chico, no solo como peso de muestra, para que el router aprenda a anticipar cuándo su propia decisión será corregida por el refutador cross-family, mejorando la calibración de confianza en el doble umbral. >> 2026-08-25: Si el delta predicho por el router es bajo pero el refutador cross-family corrige igualmente, registrar contradicción para ajustar el doble umbral en tiempo real. >> 2026-08-29: Si delta predicho bajo pero refutador corrige, registrar contradicción con timestamp; si ocurre ≥3 veces en 1h, bajar umbral superior 0.05. | estado: pendiente
- **cand-2026-08-25-02** | fecha: 2026-08-25 | vence: 2026-09-08 | lente: integracion | gist: Integrar el lente de integración con la persistencia diferida de ruido: usar los patrones de ruido post-persistencia detectados por el router en confianza baja (<0.6) como señal para ajustar dinámicamente el umbral de revisión manual en el review-gate, creando un loop donde la limpieza diferida informa al gate sobre qué umbrales realmente producen ruido, reduciendo la dependencia del clasificador en casos donde la integración de contexto es frágil. >> 2026-08-25: Si confianza <0.6 y patrón de ruido post-persistencia detectado en mismo ciclo, bajar umbral de revisión manual a 0.4 solo si ruido proviene de integración de contexto frágil. >> 2026-08-29: Si confianza <0.6 y ruido proviene de integración frágil, bajar umbral a 0.4 solo si el ruido persiste ≥2 ciclos; si es puntual, mantener 0.6. | estado: pendiente
- **cand-2026-08-25-03** | fecha: 2026-08-25 | vence: 2026-09-08 | lente: self-audit | gist: mmorch/bucketrank.py: Re-lectura de items desde disco cuando el llamador ya los tiene en memoria [estructural] — El módulo recibe `items: list[str]` como parámetro, pero el docstring dice 'graduar/ordenar un set GRANDE'. Si el llamador ya tiene los items en memoria (ej: resultado de una query), pasarlos como lista es correcto. Sin embargo, el patrón de diseño sugiere que podría releerse de disco en otros módulos. Este módulo en sí no viola ADR-0001, pero su interfaz `items: list[str]` fuerza al llamador a materializar todo en memoria, contradiciendo el espíritu de 'estado en memoria' para sets grandes. El llamador podría tener que releer de disco para construir la lista. (ver auditoria-mmorch_bucketrank.py-2026-08-25) >> 2026-08-29: Si items supera umbral de memoria (ej: 100k), aceptar iterador/streaming en lugar de list, delegando materialización al llamador. | estado: pendiente
- **cand-2026-08-25-04** | fecha: 2026-08-25 | vence: 2026-09-08 | lente: self-audit | gist: mmorch/bucketrank.py: Excepción en f.result() aborta todo el proceso sin registrar fallo [bug] — En el bucle `for f in as_completed(futs)`, si una tarea lanza una excepción no capturada por `_job` (ej: error en `f.result()` por cancelación o error interno del executor), el `except Exception` en `_job` no la cubre. Esto aborta `bucket_rank` completo, perdiendo todos los resultados ya procesados. El diseño dice 'Alineacion item<->tier preservada aunque una llamada falle' pero esto solo aplica a fallos dentro de `_grade_one`, no a fallos del pool. Debería capturarse en el bucle de `as_completed` y tratar como `None`. (ver auditoria-mmorch_bucketrank.py-2026-08-25) >> 2026-08-29: En el except del bucle as_completed, capturar excepción y reintentar la tarea una vez; si falla de nuevo, registrar warning y tratar como None. | estado: pendiente
- **cand-2026-08-25-05** | fecha: 2026-08-25 | vence: 2026-09-08 | lente: self-audit | gist: mmorch/bucketrank.py: Acoplamiento oculto al formato exacto de respuesta del modelo [principio] — El prompt exige 'exactamente: TIER: <uno de ...>' y `_extract_tier` usa regex `TIER\s*[:=]\s*([A-Za-z0-9]+)`. Si el modelo responde con formato ligeramente diferente (ej: 'Tier: S' con mayúscula, o 'TIER = S' con espacios), el regex falla y cae al tier más bajo silenciosamente. Esto viola el principio de 'seams + injection' — no hay forma de inyectar un parser alternativo. El módulo asume un contrato frágil con el modelo sin exponer un seam para adaptarse a variaciones. (ver auditoria-mmorch_bucketrank.py-2026-08-25) >> 2026-08-29: Añadir regex tolerante a mayúsculas/espacios (ej: `[Tt][Ii][Ee][Rr]\s*[:=]\s*([A-Za-z0-9]+)`) y fallback a tier medio si falla, no al más bajo. | estado: pendiente
- **cand-2026-08-29-01** | fecha: 2026-08-29 | vence: 2026-09-12 | lente: deuda | gist: agregar al router aprendido una señal de 'deuda diferida' derivada de los logs de workflow_obs.jsonl: cuando el sistema persiste con flag deuda (confianza baja), registrar el delta entre la predicción del router y el resultado observado post-revisión; usar ese delta para recalibrar el umbral de confianza por tipo de decisión, no por muestra individual, creando un ajuste periódico (cada N decisiones) que evita el ruido de muestras individuales pero captura drift sistemático. >> 2026-08-29: Si el delta acumulado por tipo supera 3 desviaciones estándar en ventana de 100 decisiones, ajustar umbral inmediatamente sin esperar N decisiones. | estado: pendiente
- **cand-2026-08-29-02** | fecha: 2026-08-29 | vence: 2026-09-12 | lente: capacidad | gist: Integrar el lente de capacidad con el hallazgo de flakiness en la suite completa: usar los registros de tests flaky (hallazgo-flakiness-suite-completa-2026-08-19) como señal para el router, entrenando un clasificador que prediga si una tarea dada tiene alta probabilidad de fallar por flakiness ambiental (no por lógica), y enrutar esas tareas a un pipeline de re-ejecución con reintentos adaptativos en vez de marcarlas como deuda técnica. Esto crea un circuito donde la capacidad de distinguir fallas reales de transitorias alimenta al router para optimizar el uso de recursos. >> 2026-08-29: Si el clasificador predice flakiness alta pero el test pasa en primer intento, registrar contraejemplo para reentrenar y ajustar umbral de reintentos adaptativos. | estado: pendiente
- **cand-2026-08-29-03** | fecha: 2026-08-29 | vence: 2026-09-12 | lente: self-audit | gist: mmorch/budget.py: Acoplamiento oculto: budget depende de la implementación interna de read_events() para su caché [estructural] — Líneas 47-48: la key del caché usa `id(events)` asumiendo que `read_events()` cachea por (mtime_ns, size) y devuelve la misma lista si el archivo no cambió. Si mañana `read_events()` cambia su estrategia de caché (ej: cachear por path+inode, o devolver una copia defensiva), el `id(events)` dejará de ser un proxy confiable de 'mismo archivo sin cambios'. Esto es exactamente el anti-patrón de 'releer del disco cuando el llamador ya tiene el dato' invertido: acá se usa el id de memoria como proxy de estado de disco, creando un contrato implícito entre módulos. La regla ADR-0001 dice que el estado debe vivir en memoria y no releerse — pero acá se está usando la identidad de memoria como si fuera estado de disco, lo que es frágil. Mejor: que `read_events()` exponga su mtime/size, o que budget calcule su propia key con stat() del path. (ver auditoria-mmorch_budget.py-2026-08-29) | estado: pendiente
- **cand-2026-08-29-04** | fecha: 2026-08-29 | vence: 2026-09-12 | lente: self-audit | gist: mmorch/budget.py: Caché de gasto mensual puede quedar stale si metrics.jsonl se rota por tamaño sin cambiar mtime [bug] — Línea 47-48: la key del caché usa `id(events)` y `len(events)`. Si el archivo metrics.jsonl se rota por tamaño (W3.4) y el nuevo archivo tiene exactamente el mismo tamaño y mtime (posible con rotación rápida o copia), `read_events()` devolverá la lista cacheada vieja (misma key de caché por mtime/size), pero `rotated_paths()` SÍ detectará el nuevo segmento. El caché de budget usará `id(events)` de la lista vieja, pero `len(events)` será diferente si el archivo nuevo tiene distinto número de eventos. Sin embargo, si el archivo nuevo tiene el MISMO número de eventos (caso borde pero posible), el caché devolverá el total viejo sin sumar el nuevo segmento rotado. El comentario en línea 47-48 asume que `id(events)` cambia cuando el archivo cambia, pero `read_events()` cachea por (mtime_ns, size) — si ambos coinciden, devuelve la MISMA lista (mismo id) aunque el archivo sea otro. (ver auditoria-mmorch_budget.py-2026-08-29) | estado: pendiente
- **cand-2026-08-29-05** | fecha: 2026-08-29 | vence: 2026-09-12 | lente: self-audit | gist: mmorch/budget.py: Violación de principio de inyección de dependencias: read_events() y rotated_paths() son hardcodeadas [principio] — Líneas 52-53: `monthly_spend()` llama directamente a `read_events()` y `rotated_paths()`. Según el principio §1 'Seams + injection = testability', las dependencias deberían inyectarse (ej: parámetro `events_loader=read_events`). Esto haría el módulo testeable sin tocar disco — hoy para testear `monthly_spend()` hay que mockear `read_events` y `rotated_paths` con monkeypatch, lo que es frágil. El módulo no tiene un `__main__` self-check (principio §1: 'Every non-trivial module leaves a runnable __main__ self-check'), y la razón principal es que no se pueden inyectar fakes sin monkeypatching. (ver auditoria-mmorch_budget.py-2026-08-29) | estado: pendiente

## Archivadas

- **cand-2026-08-12-01** | fecha: 2026-08-12 | vence: 2026-08-26 | lente: integracion | gist: graftear /speckit.analyze al engine /project — pase mmorch_adversarial_verify cross-family sobre spec+decompose antes de F1, caza contradicciones entre artefactos por centavos (origen: veredicto spec-kit 2026-06-17, re-eval 2026-08-12) >> 2026-08-14: Añadir verificación cross-family de contradicciones entre artefactos generados por spec y decompose, priorizando centavos de divergencia semántica antes del F1. >> 2026-08-15: Fusionar verificación cross-family con rollback estructural: ante contradicción, revertir automáticamente el artefacto de menor evidencia usando snapshots pre-edit. >> 2026-08-18: Añadir validación semántica de snapshots previos al rollback, detectando contradicciones con refutador antes de revertir, evitando rollbacks incorrectos. | estado: promovida
- **cand-2026-08-14-01** | fecha: 2026-08-14 | vence: 2026-08-28 | lente: deuda | gist: rollback estructural de refinements en evolve/close-loop — before/after snapshot por edit + inversion mecanica sin LLM (patron prime-agent refinement.ts:762-836); hoy revertir un finding malo es manual (~100 lineas) >> 2026-08-14: Integrar rollback estructural como tool del engine para revertir findings en lote, reutilizando snapshots por edit y validando con refutador cross-family. >> 2026-08-15: Integrar rollback estructural con refutador cross-family para auto-revertir findings en lote, usando snapshots por edit como fuente de evidencia en args_schema. >> 2026-08-18: Agregar rollback por lotes con evidencia de snapshots pre-edit verificada por refutador, y revertir findings débiles automáticamente. | estado: promovida
- **cand-2026-08-14-02** | fecha: 2026-08-14 | vence: 2026-08-28 | lente: capacidad | gist: playbooks ejecutables — campo reference {module, callable, args_schema} validado en session_skills, cierra el gap minar->ejecutar (contrato copiable de prime-agent validateEdit 680-703) >> 2026-08-14: Validar reference en session_skills con contrato copiable de prime-agent validateEdit 680-703, cerrando gap minar->ejecutar sin duplicar lógica. >> 2026-08-15: Extender contrato validateEdit para validar reference en session_skills, reutilizando snapshots de rollback estructural como fuente de evidencia para args_schema. >> 2026-08-18: Validar reference en session_skills con contrato validateEdit 680-703, reutilizando snapshots de rollback estructural como fuente de evidencia para args_schema. | estado: promovida
- **cand-2026-08-14-03** | fecha: 2026-08-14 | vence: 2026-08-28 | lente: integracion | gist: review-gate barato pre-persistencia del ingest de sesiones — 1 call DeepSeek shouldRefine antes de escribir memoria, filtra ruido (patron reviewAutoRefine prime-agent) >> 2026-08-14: Filtrar ruido pre-persistencia con shouldRefine barato, reutilizando refutador cross-family como gate para evitar escrituras innecesarias. >> 2026-08-15: Reusar filtro shouldRefine como gate para no escribir memoria en rollback estructural, evitando snapshots innecesarios de refinements ruidosos. >> 2026-08-18: Gate shouldRefine barato con presupuesto adaptativo por tipo de ruido, reusando historial de refutaciones para ajustar umbral sin LLM. | estado: promovida
- **cand-2026-08-14-04** | fecha: 2026-08-14 | vence: 2026-08-28 | lente: capacidad | gist: router aprendido — clasificador chico entrenado sobre feedback.jsonl (task signature -> modelo/umbral optimo) que reemplace/complemente el ThompsonBandit; primer entrenamiento con datos propios del loop (precedente: flywheel SimCLR le gano a bge-small) >> 2026-08-14: Entrenar router con feedback del loop + pseudo-labels del refutador cross-family para acelerar convergencia sin esperar feedback humano. >> 2026-08-15: Router aprende pseudo-labels del refutador cross-family directamente sobre feedback.jsonl, acelerando convergencia sin esperar feedback humano; reusa snapshots para validar umbrales. >> 2026-08-18: Router aprendido consume pseudo-labels del refutador cross-family; reusa snapshots para validar umbrales dinámicamente. | estado: promovida
- **cand-2026-08-14-06** | fecha: 2026-08-14 | vence: 2026-08-28 | lente: integracion | gist: Jarvis v0 — mmorch como orquestador + Home Assistant + sensores ESP32 (temperatura/presencia/consumo/camara) via MCP: medir el mundo fisico y actuar con tool-use, cero entrenamiento; percepcion con modulos preentrenados (Whisper, VLM chico); wayfinder si se acepta >> 2026-08-14: Integrar wayfinder como router físico: clasifica eventos ESP32 (presencia/consumo) para decidir tool-use en Home Assistant, reutilizando feedback.jsonl del loop. >> 2026-08-15: Wayfinder clasifica presencia/consumo para decidir tool-use en Home Assistant, usando feedback.jsonl para ajustar umbrales de acción en físico. >> 2026-08-18: Wayfinder clasifica eventos ESP32 para tool-use en HA usando feedback.jsonl como pseudo-labels de umbrales físicos, sin LLM. | estado: promovida
- **cand-2026-08-15-01** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: deuda | gist: unificar los 3 ThompsonBandit (default / _SIG_BANDIT de intuition / _WF_BANDIT de workflow_race) en una instancia con namespacing de brazos — hoy no comparten evidencia y triplican estado/decay >> 2026-08-15: Unificar ThompsonBandits en un solo bandit con namespacing por brazo, compartiendo evidencia/decay y reduciendo estado triplicado. >> 2026-08-18: Unificar bandits con namespacing por brazo y evidencia compartida, decay por namespace para priorizar exploración en dominios recientes. | estado: promovida
- **cand-2026-08-15-02** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: deuda | gist: Fusionar los 3 thompsonbandit en una sola instancia con namespacing de brazos y evidencia compartida, usando feedback.jsonl como fuente unificada de recompensas y decay global por brazo. >> 2026-08-15: Migrar feedback.jsonl a SQLite con índices por (brazo, timestamp) para decay global eficiente y consultas del router aprendido. >> 2026-08-18: Unificar feedback.jsonl en SQLite con índices (brazo, timestamp) y vistas materializadas para decay global por brazo. | estado: promovida
- **cand-2026-08-15-03** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: capacidad | gist: Añadir verificación de consistencia entre artefactos generados por el engine y los logs de feedback/workflow_obs, detectando divergencias entre lo que el sistema decide y lo que realmente ejecuta/observa, para ajustar automáticamente el router aprendido o el umbral de shouldrefine sin esperar feedback humano. >> 2026-08-15: Detectar divergencias engine/logs mediante refutador cross-family para ajustar umbral shouldRefine automáticamente, reutilizando snapshots pre-edit como evidencia. >> 2026-08-18: Añadir detector de divergencias engine/logs que ajusta umbral shouldRefine usando refutador cross-family sobre snapshots pre-edit, sin esperar feedback humano. | estado: promovida
- **cand-2026-08-15-04** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: notas-huerfanas | gist: feedback loop de auto-depuración: cuando el refutador cross-family detecta contradicciones entre artefactos y logs, en vez de solo revertir, inyectar el hallazgo como caso de test en una suite de regresión semántica que se ejecuta en el siguiente ciclo, forzando al engine a no repetir el patrón erróneo sin esperar intervención humana. >> 2026-08-15: Usar hallazgos del refutador para auto-generar tests de regresión semántica que bloqueen patrones erróneos en ciclos futuros. >> 2026-08-18: Integrar tests de regresión semántica auto-generados como gate previo al rollback, bloqueando patrones erróneos antes de revertir. | estado: promovida
- **cand-2026-08-15-05** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: deuda | gist: Entrenar un clasificador de contradicciones cross-family sobre los snapshots de rollback estructural, usando los pares (artefacto_pre_edit, artefacto_post_edit) marcados como revertidos como datos de entrenamiento negativo, y los pares que sobrevivieron como positivo; luego usar ese clasificador como gate de persistencia para nuevos hallazgos, reemplazando al refutador genérico con uno especializado en el historial real de reversiones del sistema. >> 2026-08-15: Entrenar con pares revertidos/supervivientes requiere histórico; arrancar con pseudo-etiquetas del refutador cross-family para bootstrapping del gate de persistencia. >> 2026-08-18: Entrenar gate de persistencia con pseudo-etiquetas del refutador cross-family sobre snapshots históricos, sin esperar feedback humano. | estado: promovida
- **cand-2026-08-15-06** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: integracion | gist: Integrar el router aprendido (feedback.jsonl) como gate de persistencia y selección de modelos en el pipeline de ingestion de sesiones, reemplazando el filtro shouldrefine estático por una decisión dinámica basada en la signature de la tarea, y usar los snapshots de rollback estructural como dataset de entrenamiento offline para el router (precedente: wayfinder sobre esp32), cerrando el ciclo de mejora continua sin intervención manual. >> 2026-08-15: Cerrar ciclo: feedback.jsonl + snapshots rollback como dataset offline para router, reemplazando shouldRefine estático por decisión dinámica por signature. >> 2026-08-18: Inicializar router offline con snapshots rollback como pseudo-labels, validando signature contra convergencia histórica antes de activar decisión dinámica. | estado: promovida
- **cand-2026-08-15-07** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: notas-huerfanas | gist: Entrenar un modelo de routing con feedback implícito de sesiones (no solo feedback.jsonl): usar la diferencia entre el resultado esperado y el real de cada tool-call como pseudo-label, y validar con rollback estructural (si el edit se revirtió, la predicción del router falló). >> 2026-08-15: Pseudo-labels de diferencia esperado/real por tool-call, validados con rollback estructural, alimentan dataset offline del router sin feedback humano. >> 2026-08-18: Pseudo-labels de diferencia esperado/real se filtran con refutador cross-family, previo al dataset offline del router, evitando ruido por tool-calls revertidas. | estado: promovida
- **cand-2026-08-15-05b** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: deuda | gist: SPC sobre el loop — CUSUM+EWMA (~50 lineas numpy) sobre reward medio, error-rate por modelo y maduracion de candidatas: convierte la reflexion de narrativa en ALARMAS estadisticas de drift/pseudo-progreso; chartear outcomes, jamas actividad (research escalado 2026-08 §4, mejor ratio valor/esfuerzo) >> 2026-08-18: SPC: alerta cuando CUSUM/EWMA detecta drift en reward medio, disparando revisión de candidatas. | estado: promovida
- **cand-2026-08-15-06b** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: capacidad | gist: active learning en adjudicacion — embeddings pre-filtran los 144 pares y solo la franja INCIERTA va a juicio LLM; el disenso DeepSeek-Gemini es query-by-committee gratis que ademas prioriza que guardar como dato de entrenamiento (research §1) >> 2026-08-18: Integrar disenso DeepSeek-Gemini como pseudo-labels para entrenar router aprendido, priorizando pares inciertos para feedback humano. | estado: promovida
- **cand-2026-08-15-07b** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: capacidad | gist: shadow mode para el router aprendido — predice en paralelo al bandit SIN control hasta ganarle en el eval congelado versionado (patron data-engine Tesla a escala JSONL, research §5); learning-progress bandit (reward = delta de mejora, Graves) contra el starving >> 2026-08-18: Añadir contrafactuales del bandit como pseudo-labels para el router, comparando decisiones reales vs. óptimas posteriores en eval congelado. | estado: promovida
- **cand-2026-08-15-08** | fecha: 2026-08-15 | vence: 2026-08-29 | lente: integracion | gist: MAP-Elites como dict — grilla proyecto x tipo-de-idea para candidatas: evita que un atractor monopolice (el mode-collapse del madurador ya lo mostro) manteniendo diversidad por celda, sin framework (research §3); bonus inmunologia: mutacion inversa a la afinidad en maduracion (§7) >> 2026-08-18: MAP-Elites recibe refutador cross-family como fitness de celda, penalizando contradicciones entre artefactos para diversificar sin monopolio. | estado: promovida
- **cand-2026-08-18-01** | fecha: 2026-08-18 | vence: 2026-09-01 | lente: deuda | gist: Entrenar un clasificador de ruido vs señal sobre feedback.jsonl, usando el refutador cross-family como pseudo-label, para que el review-gate barato (shouldrefine) aprenda a filtrar sin depender de un call LLM por sesión. >> 2026-08-18: Entrenar shouldRefine con pseudo-labels cross-family y mini-batching offline, sin LLM por sesión, usando feedback.jsonl como fuente única. | estado: promovida
- **cand-2026-08-18-02** | fecha: 2026-08-18 | vence: 2026-09-01 | lente: capacidad | gist: Entrenar un modelo de recompensa de preferencias (reward model) sobre feedback.jsonl, usando el refutador cross-family como anotador automático de pares (antes/después de rollback estructural), para optimizar el umbral de shouldrefine y el router aprendido de forma end-to-end con gradientes, en lugar de heurísticas separadas. >> 2026-08-18: Entrenar reward model con pseudo-labels del refutador cross-family sobre snapshots pre/post rollback, optimizando shouldRefine y router end-to-end con gradientes. | estado: promovida
- **cand-2026-08-18-03** | fecha: 2026-08-18 | vence: 2026-09-01 | lente: integracion | gist: Integrar el refutador cross-family como filtro de calidad del router aprendido: que el router, antes de decidir el modelo/umbral óptimo para una tarea, consulte al refutador para descartar pseudo-labels ruidosos o contradictorios, y que el refutador retroalimente al router con señales de consistencia semántica para ajustar sus pesos en tiempo real. >> 2026-08-18: Router consulta refutador pre-decisión; retroalimentación semántica ajusta pesos en tiempo real, descartando pseudo-labels ruidosos. | estado: promovida
- **cand-2026-08-18-04** | fecha: 2026-08-18 | vence: 2026-09-01 | lente: notas-huerfanas | gist: Integrar la verificación cross-family de contradicciones como fuente de pseudo-labels para el router aprendido, usando los snapshots de rollback estructural como evidencia de divergencia semántica entre artefactos generados por spec y decompose, y alimentando feedback.jsonl con estos casos para entrenar al router sin depender de feedback humano. >> 2026-08-18: Generar pseudo-labels del router desde contradicciones cross-family detectadas en snapshots, etiquetando la task signature del artefacto revertido como 'fallo'. | estado: promovida
- **cand-2026-08-14-05** | fecha: 2026-08-14 | vence: 2026-08-28 | lente: capacidad | gist: entrenamiento con volumen mayor — datos sinteticos gateados por refutador cross-family + mineria de historia git con reward ejecutable (replay de commits via project-build engine) + eval congelada previa + LoRA en GPU rentada; destino foggy -> wayfinder si se acepta >> 2026-08-14: Reusar snapshots de rollback estructural para replay de commits, validando convergencia con eval congelada previa y refutador cross-family. >> 2026-08-15: Replay de commits usando snapshots de rollback estructural como fuente de datos para entrenar router con pseudo-labels de refutador, acelerando convergencia sin feedback humano. >> 2026-08-18: Usar mineria de historia git para generar pseudo-labels de convergencia, entrenando el router con recompensa de replay de commits, sin refutador. | estado: promovida
- **cand-2026-08-19-01** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: capacidad | gist: Añadir un mecanismo de 'canary semántico' en el ingest de sesiones: antes de persistir cualquier artefacto nuevo, ejecutar una verificación cruzada de bajo costo entre el contenido entrante y los snapshots históricos (de rollback estructural) para detectar regresiones semánticas sutiles que el refutador actual no captura porque opera sobre pares, no sobre series temporales. >> 2026-08-19: Canary semántico: comparar hash de embeddings del nuevo artefacto contra mediana de últimos N snapshots; si divergencia > umbral adaptativo, bloquear persistencia. >> 2026-08-20: Canary semántico: usar embeddings del router para comparar contra mediana histórica, con umbral adaptativo por familia, bloqueando persistencia si divergencia > 2σ. >> 2026-08-21: Canary semántico: calcular divergencia por familia con ventana temporal móvil (no solo mediana) para detectar drift progresivo, no solo picos. | estado: promovida
- **cand-2026-08-19-02** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: integracion | gist: Integrar el router aprendido (task signature -> modelo/umbral) como gate de entrada para el review-gate barato pre-persistencia: en lugar de siempre llamar shouldrefine, el router decide si el refinamiento necesita revisión antes de persistir, usando pseudo-labels del refutador cross-family para calibrar el umbral dinámicamente. >> 2026-08-19: El router aprende a delegar al review-gate solo cuando la pseudo-confianza del refutador cae bajo umbral dinámico, calibrado por la tasa de falsos negativos observada. >> 2026-08-20: Entrenar el router con pseudo-labels del refutador cross-family para predecir falsos negativos, ajustando el umbral dinámico por familia y tasa observada. >> 2026-08-21: Router calibra umbral dinámico por familia usando tasa de falsos negativos del refutador, no solo pseudo-confianza. | estado: promovida
- **cand-2026-08-19-03** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: notas-huerfanas | gist: Añadir un 'filtro de ruido temporal' al ingest de sesiones: antes de persistir memoria, comparar el timestamp del evento con el contexto de la sesión activa; si el evento es redundante con uno reciente (misma firma semántica en <5 min), descartarlo sin llamar a shouldrefine (ahorro de costo y latencia). Esto complementa el gate barato existente, pero ataca la duplicación temporal, no la calidad del contenido. >> 2026-08-19: Fusionar con canary: el filtro temporal descarta duplicados; el canary semántico detecta regresiones, ambos pre-persistencia sin shouldrefine. >> 2026-08-20: Fusionar filtro temporal y canary semántico: descartar duplicados <5min y bloquear divergencia >2σ, ambos sin shouldrefine, en pipeline único pre-persistencia. >> 2026-08-21: Filtro temporal y canary semántico comparten pipeline; el filtro descarta duplicados, el canary detecta drift; ambos pre-persistencia sin shouldrefine. | estado: promovida
- **cand-2026-08-19-04** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: integracion | gist: graft de loushang: Sistema de sesiones persistentes con resume/fork/export — Un módulo de sesiones que serializa el estado completo de una conversación/ejecución (mensajes, contexto, herramientas, productos de trabajo) a disco, permitiendo reanudar, bifurcar y exportar. Incluye diagnóstico de integridad y recuperación ante corrupción. (aplica a mmorch: el orquestador multi-modelo necesita persistir el estado de las iteraciones de ideas, las decisiones del bandit y el historial de verificación cross-family para poder reanudar experimentos largos sin perder contexto.; ver nota minado-loushang-2026-08-19) >> 2026-08-19: Serializar snapshots de sesiones con hash de estado; fork crea rama sin mutar original; export incluye manifiesto de dependencias. >> 2026-08-20: Fork de sesión: clonar solo delta de estado (mensajes desde último snapshot) con reescritura de punteros, preservando integridad y reduciendo I/O. >> 2026-08-21: Fork con hash de estado incremental: reescribir punteros solo si el delta cambia, evitando I/O redundante en snapshots idénticos. | estado: promovida
- **cand-2026-08-19-05** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: integracion | gist: graft de loushang: Catálogo de modelos con registro y resolución por proveedor — Un registro central de modelos AI con metadatos (proveedor, capacidades, costo, límites de contexto) y una función de resolución que selecciona el modelo apropiado según la tarea, con fallback y listado de modelos disponibles. (aplica a mmorch: el orquestador necesita un catálogo unificado de modelos para el bandit multi-brazo, permitiendo registrar nuevos modelos sin modificar el núcleo y resolviendo el mejor modelo por tarea/costo.; ver nota minado-loushang-2026-08-19) >> 2026-08-19: Registrar modelos con costo real por llamada y latencia observada; resolver por presupuesto y SLA, no solo capacidad. >> 2026-08-20: Añadir registro de costo por modelo y presupuesto por tarea; resolver por SLA y costo, con fallback a modelo más barato si excede presupuesto. >> 2026-08-21: Registrar latencia y costo real por proveedor+modelo; resolver por presupuesto y SLA con fallback a más barato si excede. | estado: promovida
- **cand-2026-08-19-06** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: integracion | gist: graft de loushang: Sistema de extensiones con hooks, tools y recursos dinámicos — Un mecanismo de extensión a nivel de proyecto donde código Python externo puede contribuir hooks (eventos del ciclo de vida), tools personalizadas, recursos dinámicos y comandos, sin modificar el núcleo. Incluye resolución de precedencia y colisiones. (aplica a mmorch: el flywheel de entrenamiento y los proyectos del dueño (portfolio, apps Tauri, sistemas de estudio) pueden extender el orquestador con tools específicas de dominio sin tocar el código base.; ver nota minado-loushang-2026-08-19) >> 2026-08-19: Resolver colisiones de tools por firma y namespace, con precedencia explícita por prioridad de proyecto. >> 2026-08-20: Resolver colisiones de tools por firma y namespace con precedencia por prioridad de proyecto, permitiendo override de tools base vía registro explícito. >> 2026-08-21: Precedencia por prioridad de proyecto con override explícito; registry versionado con rollback atómico para colisiones de hooks y tools. | estado: promovida
- **cand-2026-08-19-07** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: capacidad | gist: Integrar un 'selector de lente adaptativo' en el ingest: antes de persistir, el sistema evalúa la firma semántica del artefacto entrante y selecciona dinámicamente el lente más barato que cubra el tipo de riesgo (canary semántico vs. filtro temporal vs. refutador cross-family), en lugar de aplicar todos secuencialmente, reduciendo latencia y costo en flujos de alta frecuencia. >> 2026-08-19: El selector evalúa firma semántica y elige el lente más barato según tipo de riesgo, pero el canary temporal ya cubre duplicados; fusionar con filtro temporal para evitar redundancia. >> 2026-08-20: El selector de lente adaptativo prioriza por costo, no por riesgo; incluir umbral por familia y fallback a canary completo si firma ambigua. >> 2026-08-21: Fusionar selector de lente con filtro temporal: si firma es duplicado reciente, descartar sin evaluar otros lentes; solo evaluar canary semántico si no es duplicado. | estado: promovida
- **cand-2026-08-19-08** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: self-audit | gist: mmorch/ablation.py: Acoplamiento oculto: _verify depende de _SKEPTIC_SYSTEM y _parse_verdict de patterns.py sin inyección [estructural] — Líneas 38-47: _verify importa _SKEPTIC_SYSTEM y _parse_verdict directamente de patterns.py. Si patterns.py cambia el formato del system prompt o el parser de veredictos, ablation.py se rompe silenciosamente sin error de importación. El principio de inyección de dependencias (seam + injection) exige que estos sean parámetros inyectables. Además, _SKEPTIC_SYSTEM es un nombre con prefijo _ (privado) que se está usando desde otro módulo — señal de que el acoplamiento no fue diseñado intencionalmente. (ver auditoria-mmorch_ablation.py-2026-08-19) >> 2026-08-20: Inyectar _SKEPTIC_SYSTEM y _parse_verdict como parámetros con defaults desde patterns.py, usando seam explícito en _verify. >> 2026-08-21: Inyectar _SKEPTIC_SYSTEM y _parse_verdict como parámetros con defaults, además de un seam de override para tests. | estado: promovida
- **cand-2026-08-19-09** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: self-audit | gist: mmorch/ablation.py: by_case almacena artifact truncado como label, duplicando datos que el llamador ya tiene [estructural] — Línea 66: `by_case.append({"label": c.label or c.artifact[:30], ...})` — si el llamador ya tiene los casos en memoria (los pasó como argumento), almacenar el artifact truncado en el resultado duplica información. El llamador puede reconstruir el label desde `cases[i].label` o `cases[i].artifact[:30]`. Esto viola la regla de 'estado en memoria, no releer disco' — aunque aquí es 'no duplicar en memoria'. El resultado debería referenciar el índice del caso, no copiar el dato. (ver auditoria-mmorch_ablation.py-2026-08-19) >> 2026-08-20: Devolver índice de caso en by_case en vez de duplicar label truncado, permitiendo al llamador reconstruir sin copia. >> 2026-08-21: Devolver índice de caso en by_case; el llamador reconstruye label desde cases[i].label, sin duplicar artifact. | estado: promovida
- **cand-2026-08-19-10** | fecha: 2026-08-19 | vence: 2026-09-02 | lente: self-audit | gist: mmorch/ablation.py: División por cero en lat_avg cuando cases está vacío [bug] — Línea 76: `lat_avg=round(sum(lats) / n, 2) if n else 0.0` — el guard `if n` protege la división, pero `sum(lats)` sobre una lista vacía devuelve 0.0, lo cual es correcto. Sin embargo, la línea 74 `accuracy=round(correct / n, 4) if n else 0.0` tiene el mismo patrón y es correcta. El bug real está en la línea 76: si `cases` está vacío, `n=0`, y aunque el guard evita el ZeroDivisionError, el resultado `0.0` es engañoso — debería ser `None` o un valor que indique 'no data', no 0.0 que sugiere 0% de precisión. (ver auditoria-mmorch_ablation.py-2026-08-19) >> 2026-08-20: Devolver lat_avg=None si n=0, y en el caller mostrar 'sin datos' en reportes y dashboards, evitando 0.0 engañoso. >> 2026-08-21: lat_avg=None si n=0; caller muestra 'sin datos' en reportes/dashboards, evitando 0.0 engañoso. | estado: promovida
- **cand-2026-08-20-01** | fecha: 2026-08-20 | vence: 2026-09-03 | lente: capacidad | gist: Incorporar un 'detector de regresión de capacidad' post-persistencia: tras cada snapshot, comparar el rendimiento del sistema (latencia, tasa de acierto, cobertura de tareas) contra la línea base histórica usando el mismo mecanismo de canary semántico, pero sobre métricas operativas en lugar de embeddings; si se detecta degradación > umbral, activar automáticamente un rollback estructural parcial del último cambio. >> 2026-08-20: Añadir correlación entre métricas operativas y embeddings canarios: si latencia/cobertura degradan sin divergencia semántica, marcar rollback estructural por regresión de comportamiento. >> 2026-08-21: Post-rollback, re-ejecutar canary semántico para validar recuperación; si persiste degradación, escalar a revisión manual de cambios estructurales acumulados. | estado: promovida
- **cand-2026-08-20-02** | fecha: 2026-08-20 | vence: 2026-09-03 | lente: self-audit | gist: mmorch/adjudicate.py: Relectura innecesaria de archivos del disco tras actualizar frontmatter [estructural] — En run_incremental, después de llamar a _update_frontmatter(note_path, strong_projects), se relee el archivo con Path(note_path).read_text() para recalcular el hash. Esto viola el principio ADR-0001 (estado en memoria, no releer disco). El llamador YA tiene note['content'] en memoria y _update_frontmatter podría devolver el nuevo contenido. Además, esta relectura es frágil: si el archivo cambia entre la escritura y la lectura (race condition), el hash calculado no corresponderá al contenido real que se escribió. (ver auditoria-mmorch_adjudicate.py-2026-08-20) >> 2026-08-21: Hacer que _update_frontmatter devuelva el nuevo contenido para que run_incremental lo use, eliminando la relectura del disco. | estado: promovida
- **cand-2026-08-20-03** | fecha: 2026-08-20 | vence: 2026-09-03 | lente: self-audit | gist: mmorch/adjudicate.py: Duplicación de lógica de reconstrucción de by_project [estructural] — by_project se reconstruye desde state['pairs'] al final de cada corrida, pero strong_results (que contiene los mismos datos en memoria) ya tiene toda la información necesaria. Se está duplicando la fuente de verdad: strong_results en memoria y state['by_project'] en disco. Si alguien modifica la lógica de filtrado de strong en un lugar pero no en el otro, los resultados divergirán silenciosamente. La reconstrucción desde pairs es correcta pero redundante — strong_results ya es la fuente en memoria. (ver auditoria-mmorch_adjudicate.py-2026-08-20) >> 2026-08-21: Derivar by_project directamente de strong_results al persistir, eliminando reconstrucción desde pairs; strong_results como única fuente de verdad. | estado: promovida
- **cand-2026-08-20-04** | fecha: 2026-08-20 | vence: 2026-09-03 | lente: self-audit | gist: mmorch/adjudicate.py: Acoplamiento oculto con el formato exacto de frontmatter [estructural] — _update_frontmatter asume que el frontmatter YAML se serializa con yaml.dump y que el cuerpo comienza inmediatamente después del segundo '---'. Si otro módulo escribe frontmatter con un formato diferente (por ejemplo, con '---' al final del frontmatter en la misma línea, o con BOM), esta función corromperá el archivo. El módulo no valida que el frontmatter parseado sea un diccionario antes de asignar frontmatter['applies_to'] — si yaml.safe_load devuelve una lista o string, fallará con TypeError en lugar de manejar el caso. (ver auditoria-mmorch_adjudicate.py-2026-08-20) >> 2026-08-21: Validar que yaml.safe_load devuelva dict; si no, abortar con error descriptivo antes de asignar frontmatter['applies_to']. | estado: promovida
- **cand-2026-08-21-01** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: deuda | gist: añadir un mecanismo de 'canary estructural' post-persistencia: tras cada snapshot de rollback estructural, ejecutar una verificación asíncónica de integridad de grafos (detectar ciclos no deseados, nodos huérfanos, o dependencias rotas entre artefactos) usando el log de workflow_obs.jsonl como fuente de verdad, y si se detecta una anomalía estructural, disparar automáticamente una reversión local al último snapshot válido — esto complementa el canary semántico pre-persistencia al atacar regresiones de topología, no de contenido, y aprovecha la auditoría existente sin costo adicional en el hot path. >> 2026-08-21: Canary estructural: verificar integridad post-snapshot contra workflow_obs.jsonl; si anomalía, revertir al último snapshot válido, complementando canary semántico pre-persistencia. | estado: promovida
- **cand-2026-08-21-02** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: capacidad | gist: Incorporar un 'contador de ciclos de vida' en los artefactos persistidos: antes de escribir cualquier producto de trabajo, registrar el número de veces que fue revisado/refutado y su historial de mutaciones; si un artefacto alcanza N revisiones sin cambio semántico neto (delta de embedding < umbral), marcarlo como 'estable' y priorizar su reutilización en el router, reduciendo llamadas innecesarias a shouldrefine y mejorando la eficiencia del bandit al explotar conocimiento ya validado. >> 2026-08-21: El contador de ciclos de vida se integra al canary semántico: si un artefacto estable (N revisiones sin delta) supera el umbral de drift, se desmarca y se reenvía a shouldrefine. | estado: promovida
- **cand-2026-08-21-03** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: notas-huerfanas | gist: Añadir un 'veredicto de continuidad' al pipeline de verificación: tras cada veredicto del refutador cross-family sobre un artefacto, registrar un hash del estado del sistema (configuración activa, versiones de módulos, umbrales dinámicos) junto con el resultado; al persistir un nuevo artefacto, comparar el hash de estado actual contra el histórico para detectar si el contexto de verificación cambió significativamente (p.ej., umbrales recalibrados, modelo reemplazado), lo que invalida veredictos previos y activa re-verificación selectiva solo de artefactos afectados, evitando falsas confianzas por deriva de configuración. >> 2026-08-21: Al detectar hash de contexto cambiado, invalidar solo veredictos cuyo hash de configuración difiera; re-verificar selectivamente esos artefactos. | estado: promovida
- **cand-2026-08-21-04** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/arbitration.py: Relectura de disco en pending_recheck() cuando el llamador ya tiene los datos [estructural] — pending_recheck() llama a _read(path) que relee el archivo JSONL completo del disco. Pero el orquestador que llama a esta función típicamente ya tiene los registros en memoria (p.ej. después de llamar a stats() o a log()). Esto viola el ADR 0001 (estado en memoria, no releer disco). El módulo debería aceptar una lista de registros como parámetro opcional (p.ej. rows=) para que el llamador pueda pasar los datos que ya tiene, evitando I/O redundante. La firma actual fuerza una relectura innecesaria cada vez que se consulta la cola de re-check. (ver auditoria-mmorch_arbitration.py-2026-08-21) >> 2026-08-21: Pending_recheck(rows=None) acepta registros en memoria; si None, relee del disco. Orquestador pasa stats()/log() ya leídos. | estado: promovida
- **cand-2026-08-21-05** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/arbitration.py: Truncamiento silencioso de evidencia en log() [bug] — En log(), el parámetro evidence se trunca a 200 caracteres (evidence[:200]) sin advertencia. Si el orquestador pasa una evidencia más larga (p.ej. un probe con salida extensa), el registro guarda una versión incompleta. Luego, stats() calcula 'dismissed_without_evidence' basándose en si el campo está vacío, pero un registro con evidencia truncada a 200 chars sigue contando como 'con evidencia'. Esto puede inflar la tasa de descartes con evidencia y ocultar descartes que en realidad tenían evidencia insuficiente pero que fue cortada. El truncamiento debería al menos registrar un flag o el largo original. (ver auditoria-mmorch_arbitration.py-2026-08-21) >> 2026-08-21: Registrar metadata en log(): largo_original y flag truncado; stats() separa evidencia completa de truncada al calcular dismissed_without_evidence. | estado: promovida
- **cand-2026-08-21-06** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/arbitration.py: Acoplamiento oculto: el orquestador debe conocer el formato interno de los registros [principio] — El docstring dice 'El re-check es juicio del orquestador; acá solo se surfacea la cola'. Pero pending_recheck() devuelve dicts crudos con campos internos (ts, critique, verdict, reason, source, evidence, rechecked). El orquestador que hace el re-check debe saber que la clave 'evidence' existe y qué significa, y debe mutar el dict para marcar 'rechecked': True. Esto es un acoplamiento oculto: si el módulo cambia el formato interno (p.ej. renombra 'evidence' a 'proof'), el orquestador se rompe silenciosamente. El módulo debería exponer una función mark_rechecked(rec) o devolver objetos con métodos, no dicts crudos. (ver auditoria-mmorch_arbitration.py-2026-08-21) >> 2026-08-21: Agregar método `mark_rechecked(rec)` que encapsule la mutación interna y valide el formato, eliminando el acoplamiento del orquestador. | estado: promovida
- **cand-2026-08-21-07** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: deuda | gist: Integrar el filtro de ruido temporal y el canary semántico en un pipeline unificado de pre-persistencia que además registre el 'costo de oportunidad' de cada bloqueo/descarte: en lugar de solo bloquear o descartar, calcular el ahorro de tokens/costo de cómputo evitado (por no llamar a shouldrefine) y el riesgo de pérdida de información (por descartar duplicados o bloquear regresiones), alimentando un dashboard de retroalimentación que ajuste los umbrales adaptativos en tiempo real según la relación costo-beneficio observada. >> 2026-08-21: Calcular ahorro de tokens por filtro temporal/canary y riesgo de pérdida por descarte, ajustando umbrales adaptativos según costo-beneficio observado. | estado: promovida
- **cand-2026-08-21-08** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: capacidad | gist: Añadir un 'pipeline de verificación de integridad semántica post-persistencia' que, tras cada snapshot de sesión (fork/export), ejecute una validación asincrónica de consistencia entre el manifiesto de dependencias y los artefactos referenciados, detectando referencias huérfanas o versiones desalineadas que puedan corromper la reanudación futura, complementando el canary pre-persistencia. >> 2026-08-21: Verificar checksums de artefactos contra manifiesto; si mismatch, re-generar snapshot desde estado previo y reintentar fork/export. | estado: promovida
- **cand-2026-08-21-09** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: notas-huerfanas | gist: Integrar el filtro temporal y el canary semántico con el router aprendido para predecir la relevancia futura del artefacto: antes de persistir, el router no solo decide si se necesita revisión, sino que también estima la probabilidad de que el artefacto sea recuperado/utilizado en futuras sesiones (basado en patrones de acceso histórico por familia y firma semántica); si la probabilidad es baja y el artefacto no es candidato a revisión, se descarta directamente sin persistir, reduciendo ruido acumulativo y costo de almacenamiento. >> 2026-08-21: Registrar métricas de descarte (probabilidad baja + no revisión) por familia para calibrar umbrales y auditar pérdida de artefactos potencialmente útiles. | estado: promovida
- **cand-2026-08-21-10** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/architecture.py: god_module_candidates relee archivos que import_graph ya parseó [estructural] — Líneas 47-48 vs 55-56: import_graph ya lee y parsea cada archivo con ast.parse(f.read_text()). Luego god_module_candidates vuelve a leer cada archivo con f.read_text() para contar LOC. Esto viola el ADR 0001-estado-en-memoria-no-releer-disco.md — el contenido ya está en memoria (o al menos el AST), pero se relee del disco. Para un repo con 50 módulos, son 50 lecturas redundantes de disco. Peor: si el archivo cambia entre la primera y segunda lectura (race condition en CI), el LOC contado no corresponde al AST parseado. Fix: hacer que import_graph devuelva también el LOC, o que god_module_candidates reciba los ASTs ya parseados. (ver auditoria-mmorch_architecture.py-2026-08-21) | estado: promovida
- **cand-2026-08-21-11** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/architecture.py: co_change_pairs cuenta commits vacíos como cambios [bug] — Líneas 88-96: Cuando un commit toca archivos fuera de mmorch/ (ej: docs/, tests/), el parser agrega un set vacío a commits. El filtro len(c) <= _SWEEP_MAX_FILES no elimina sets vacíos (0 <= 8 es true). Esto infla solo y junto con commits que no tocan ningún archivo relevante, distorsionando los ratios. Un commit que solo toca README.md produce actual = set() que se agrega a commits. Si un commit toca 1 archivo de mmorch/ y 10 de docs, el set tiene 1 elemento, pasa el filtro, y cuenta como co-cambio legítimo cuando en realidad fue un commit de docs con un fix incidental. Fix: filtrar sets vacíos antes del filtro de barrido: commits = [c for c in commits if c and len(c) <= _SWEEP_MAX_FILES] (ver auditoria-mmorch_architecture.py-2026-08-21) | estado: promovida
- **cand-2026-08-21-12** | fecha: 2026-08-21 | vence: 2026-09-04 | lente: self-audit | gist: mmorch/architecture.py: _mutates no detecta mutación vía setattr o globals()[name] [bug] — Líneas 130-141: El regex solo detecta NAME[...] = y NAME.metodo(). Pero Python permite mutar globals sin esas sintaxis: globals()['CONFIG'].update({...}) o setattr(sys.modules[__name__], 'CONFIG', {...}) no son detectados. Un test que haga globals()['SHARED_STATE'].clear() pasa desapercibido. Es un caso borde, pero el módulo se vende como 'chequeo mecánico determinista' — un falso negativo aquí es exactamente el tipo de contaminación que busca prevenir. Fix: añadir detección de globals()[ y setattr( con el nombre como string: globals_call = re.compile(rf"globals\(\)\s*\[\s*['\"]{name}['\"]\s*\]") (ver auditoria-mmorch_architecture.py-2026-08-21) | estado: promovida
