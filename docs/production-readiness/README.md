# Production-readiness — snapshots, not SSOT

These notes are **dated research consolidations**. They describe the product as it
looked on the date in each file. They are not a source of truth and must not be
copied into GOAL / CLAUDE / AGENTS.

- Living “what exists”: `docs/generated/catalog.md` + the code.
- Living status of orchestra members: `mmorch/nodes.py`.
- Defects that still matter: promote to bd, don’t keep editing this matrix.

Map: `docs/SOURCES.md`.
